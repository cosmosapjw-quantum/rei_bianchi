# CURRENT HANDOFF PROMPT — rei_bianchi / BASS

Treat this as a durable continuation of the public `rei_bianchi` repository
inside BASS. Repository bytes, hashes, ledgers and receipts are authoritative;
transcript claims are not evidence.

## Before calculation

1. Read `PROJECT_STATE.json`, `docs/science/current_00_READ_FIRST.md`, this file,
   `docs/provenance/DURABLE_STAGE_LEDGER.csv`, `external/rec_bianchi.lock.json`,
   and `external/REC_BIANCHI_MONITORING_POLICY.md`.
2. Run `python scripts/verify_repo.py`, current-stage tests, exact validator and
   `sha256sum -c` in the current stage.
3. Refresh read-only refs. Record exact SHAs or explicit unavailable status.
   An updated `rec_bianchi` SHA requires deliberate adapter/input-lock review.
4. Create a new durable directory, input lock, state, receipts, manifest and
   `SHA256SUMS` before calculation.

## Conventions

- metric `(-,+,+,+)`; `epsilon_123=+1`;
- explicit `c`, `hbar`, `k_B`;
- homogeneous background, tetrad and 1+3 formalisms;
- eventual all-11 Bianchi coverage, finite tilt and nonlinear shear.

## Current durable verdict

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-R1-R1-R1-EVALUATION-SITE-SPARSE-GENERATOR-VALIDATED-MPRK22-SDIRK2-DISCRETE-MAP-ENCLOSURE-LOCK
DURABLE_FAIL_CLOSED_R2_R1A_R1_R1_R1_R1_FOUR_SITE_PRIMAL_PARITY_AND_LOCAL_IMPLICIT_CERTIFICATES_PASS_CROSS_SITE_STATE_FEEDBACK_REMAINDER_EVENT_RESTART_AND_SET_LEDGER_UNCLOSED
```

### Closed in this stage

- four-site wrapper is bitwise primal-equivalent in all three lanes;
- `552960/552960` frozen-state H/He population blocks certified;
- maximum population Krawczyk row-sum `0.031102095838177812`;
- implicit tangent complex-step mismatch `3.3717708261732545e-15`;
- `276480/276480` fixed-heating thermal roots certified;
- maximum thermal image/tube ratio `9.9706373090339612e-05`;
- exact H/He and owner-normalization identities pass.

### Still open

An admissible stagewise control escapes the static hull in `x_HeIII` by
`6.9791494632098772e-12`. No outward remainder
currently composes the four independent sites through state feedback, owner
normalization and thermal coupling. Table-event restart and set-valued ledgers
are not certified.

Public repository pages were observed, but native Git/API DNS failed and exact
current SHAs were unavailable. No push, PR or merge is inherited. The
`rec_bianchi` web page reports a v0.70-P0 development reference; no numerical
state/rate/history is imported and no adapter review is started.

## Next exact stage

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-R1-R1-R1-R1-CROSS-SITE-STATE-FEEDBACK-REMAINDER-AND-TABLE-EVENT-LOCK
```

1. Keep independent local `(v,f,vf)` generators at all four sites.
2. Propagate outward cross-site and state-feedback remainder through the actual
   MPRK22/SDIRK2 composition.
3. Keep owner normalization as local diagonal plus named low-rank reductions.
4. Localize Hummer-Seaton knot events before commit and restart fixed topology.
5. Close nuclei, photons, resolved heat, unresolved OTS, escaped and total
   energy as set inclusions.
6. Contain the 24 inherited trajectories, the stagewise witness and interior
   falsification samples in all three lanes.
7. Authorize the first canonical interval only if all public widths are below
   `2e-3`.

## Prohibited

No clipping, silent source-table extrapolation, unsupported temporal coherence,
owner reassignment, dense full Jacobian, post-hoc lane selection, recombination
surrogate, CAMB transfer or Bianchi-background coupling in this next stage.
