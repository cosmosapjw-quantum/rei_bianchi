# CURRENT HANDOFF PROMPT — rei_bianchi

Treat this as a durable continuation of the private `rei_bianchi` project. Repository files, hashes, ledgers and receipts are authoritative; transcript claims are not evidence.

## Before calculation

1. Read `PROJECT_STATE.json`, `docs/science/current_00_READ_FIRST.md`, this file, `docs/provenance/DURABLE_STAGE_LEDGER.csv`, `external/rec_bianchi.lock.json`, and `external/REC_BIANCHI_MONITORING_POLICY.md`.
2. Run `python scripts/verify_repo.py` and the current stage tests.
3. Run `scripts/update_rec_bianchi_lock.sh`; record the exact remote HEAD or an explicit unavailable status.
4. Verify every next-stage input hash and create the durable directory, input lock, state, receipts, manifest and `SHA256SUMS` before calculation.

## Conventions

- metric `(-,+,+,+)`; `epsilon_123=+1`;
- explicit `c`, `hbar`, `k_B`;
- homogeneous background, tetrad and 1+3 formalisms;
- all 11 Bianchi types ultimately supported; finite tilt and nonlinear large shear.

## Current durable verdict

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-VALIDATED-CONTINUOUS-BRANCH-DIFFERENTIAL-INCLUSION-ENCLOSURE-LOCK
DURABLE_FAIL_CLOSED_R2_R1A_R1_CONSTANT_ORTHANT_EXCLUDED_COMPONENTWISE_BOX_WRAPPING_CROSSES_SOURCE_TABLE_BOUNDARY_AFFINE_TAYLOR_MODEL_LOCK_AUTHORIZED
```

A robust sign reversal of `d(d log T/dt)/d x_HII` excludes every constant diagonal orthant comparison theorem. An outward-rounded componentwise interval-Picard implementation passes an analytic scalar control, but partitions 16, 32 and 64 all lose state/parameter correlations and expand through the upper Hummer--Seaton source boundary before any project subsegment is certified. This is a wrapping/dependency failure of the box representation, not a physical no-go.

The previous 24 numerical realization endpoints remain narrow and pass all physical ledgers, but they are regression evidence only. Production history, production node chemistry, `R2C-R2` and `B2C2B` remain unauthorized.

`rei_bianchi/main` was connector-verified at `fdad2e141afe056a9a5e672c7870c021c2e2558f`. `rec_bianchi/main` was connector-verified at `2d777b1c7e56dcdf1e17feb1f728410ea0792df8` (PR-05C2C0/v0.65 scalar theory contract). Import only ownership, transaction and bounded-no-go semantics; do not import numerical rates, states or accepted history.

## Next exact stage

Execute:

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-R1-AFFINE-SET-PARAMETERIZED-TAYLOR-MODEL-CONTINUOUS-BRANCH-ENCLOSURE-LOCK
```

1. Keep the 26-event registry, MPRK22(1), Alexander-SDIRK2, canonical forcing, owner law, exact He II Ly-alpha heating, unresolved OTS ledger and 24 regression endpoints fixed.
2. Replace plain componentwise boxes with a correlation-preserving affine/Taylor-model set parameterization.
3. State the admissible branch-function set explicitly; any regularity/correlation assumption beyond the source bounds is a named source-extension axiom.
4. Remove H/He invariant directions analytically and retain owner-normalization coupling as explicit low-rank generators.
5. Localize every Hummer--Seaton table-knot event and restart the validated representation there.
6. Validate point parity, all 24 endpoints, positivity, H/He nuclei, photon ownership, resolved thermal, unresolved OTS, escaped and total-energy ledgers.
7. Authorize the uncertainty-qualified first canonical interval only if all three shape lanes are enclosed continuously and every public width is below `2e-3`.

## Prohibited

No clipping, silent table extrapolation, corner-as-proof, owner reassignment, `kappa=J/Phi` inversion, cloud/geometry inversion, per-node fitting, post-hoc lane selection, recombination surrogate, CAMB transfer or Bianchi feedback.

The user performs remote push. Never claim a push without successful `git push` and post-push `git ls-remote` verification.
