# When to promote a workflow to a skill

다음 조건을 만족하면 긴 prompt 반복 대신 skill로 만든다:

- 같은 절차를 3회 이상 사용
- 같은 실수를 2회 이상 교정
- 입력/출력과 실행 명령이 안정적
- 검증 기준이 고정
- 매 단계 새로운 인간 판단이 필요하지 않음

한 skill은 한 가지 일에 집중한다. 설명은 언제 써야 하고 쓰지 말아야 하는지 명확히 한다.
