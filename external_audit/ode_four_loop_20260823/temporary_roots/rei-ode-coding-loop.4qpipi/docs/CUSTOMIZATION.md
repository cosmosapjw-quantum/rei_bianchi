# Customization

- repo 고유 build/test 명령은 `AGENTS.md`에 짧게 추가한다.
- 모듈별 규칙은 하위 `AGENTS.md`에 둔다.
- scientific contract는 실제 프로젝트 값으로 반드시 채운다.
- validation matrix는 CI 또는 local commands에 연결한다.
- 반복 workflow만 skill로 승격한다.
- 새 tool/MCP는 실제 수동 loop를 제거할 때만 추가한다.
