# Codex → Work research-results handoff

commit/branch/worktree, changed behavior, commands/environment, validation results, baseline delta, numerical uncertainty, failed approaches, limits, artifact paths, reproduction commands, follow-up questions를 정리한다. 사실, 해석, 추론을 구분하고 미실행 검증을 성공처럼 기록하지 않는다.
