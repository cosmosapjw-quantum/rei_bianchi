# RESEARCH OUTCOME

## Outcome first

The supplied coding harness was executed as a **research-only** loop at REI Bianchi HEAD `111b6ace750e36e218df7fc9626c6bad2ec19971`. The archive's instructions governed method, not user scope or scientific truth. No target file, test, dependency, baseline, reference, runtime result, package, or untracked bundle was changed.

Final design decision: **`HOLD` H2/LCV-ODE as `CONDITIONAL_ARCHITECTURE_NOT_IMPLEMENTATION_READY`**.

- H1, a localized patch, is rejected: continuous defect/global/QoI, all-root event/rebuild, independent-reference, controller, filesystem, process, and custody obligations cross the local numerical kernel boundary.
- H2, a layered contract/validation-first successor stage, is the best and smallest-scope research direction among the three, but still depends on seven unresolved owner/scientific/numerical decisions.
- H3, wholesale replacement, is held in reserve only if an H2 fatal falsifier later shows that the successor interfaces cannot be integrated or bounded.

## Decisive current evidence

| Evidence | Result | Claim tier |
|---|---|---|
| Active-stage unit suite | 49 tests PASS in 8.414 s | compatibility diagnostic only |
| Exact signed subnormal cancellation | exact result not contained | current numerical contract FAIL |
| Two exact-solution Krawczyk systems | `certified=True`, exact solution outside returned bounds | false-certificate FAIL |
| Pure-He II coefficient | current `0.006241` vs convention-derived `0.079`, ratio `YHE=0.079` | formula/binding FAIL pending owner convention |
| Live runtime vs pins | NumPy `2.4.2` vs `2.3.5`; pandas `3.0.0` vs `2.2.3`; SciPy matches `1.17.0`; JAX absent | exact runtime identity FAIL for this shell |
| Tracked parity evidence | current `13/22`; helper manual/disconnected while older 22/22 language remains | authenticated admission FAIL |
| Source/call graph | stop-only active table event, no active continuous defect/global/QoI, same-lineage BDF replay, post-hoc stream buffering, incomplete terminal FSM | static/source-bound retained blockers; no driver exploit claimed |

The 49 green tests therefore cannot discharge the exact false-containment/certificate witnesses or the missing cross-boundary contracts.

## Completeness and its ceiling

- Every frozen blocker ID 1–32 appears exactly once in the mechanical crosswalk.
- Every seed S-01–S-12 maps to at least one current code surface and proposed gate.
- Corrected interpretation: **32/32 inventoried = 31 mandatory correctness/custody obligations + #18/S-07 as a nonblocking performance opportunity**. It must pass its own gate before any performance claim.
- #19 is `PARTIAL_NARROWED`: useful no-extrapolation/domain checks already exist; residual raw-Gamma/dense-output/API surfaces remain, but active out-of-range evaluation was not demonstrated.
- This is completeness relative to the frozen inventory and inspected A/P/V/C roots. It is not a proof that no unknown defect exists; dynamic/generated/alternate-name paths and unexecuted behaviors remain a ceiling.

## Corrected H2 architecture

All existing A/P/V/C/S/R2A/U/B stages are immutable predecessor/forensic evidence. A future implementation, if separately authorized, must use only the proposed new successor:

`stages/Bianchi_Reionization_Stage_P0_5_B2C2B0C_R2C_R1B_R2B_R2A_R2_R1A_R1_R1_R1_R1_R1_R1_R1_LCV_ODE_CORRECTED_SCIENCE_V2_CANDIDATE`

The successor has new v2 state/result/control/parity/package schemas, manifest, input lock, runtime ABI, routing policy, and hashes. V1 remains `FORENSIC_COMPATIBILITY_V1`; v2 remains `CORRECTED_SCIENCE_V2_CANDIDATE`; blocked evidence has a third closed-world identity. V1 can never serve as a silent corrected-science fallback.

The dependency order is:

1. I-0 v2 schemas, characterization, typed result and admission vocabulary.
2. I-1 verified arithmetic, corrected formula/constraints, theorem-matched certificate and fail-closed consumer.
3. I-2 independent photon/energy ledgers and distinct discrepancy/defect/global/event/QoI error evidence.
4. I-3 all-root hybrid event isolation, ambiguity status, rollback/rebuild/restart.
5. I-5/I-6 explicit FSM, atomic generations, exact ABI, bounded streaming/resources, containment, controller-consumed parity and linear ancestry.
6. I-7 additive corrected BDF/reference lineage and independent oracle.
7. I-4 structured rank-`<=3` optimization only after dense correctness; optional for correctness, mandatory for a performance claim.

Corrected admission is structurally disabled while any mandatory slice is shadow-only. Ambiguous event/certificate, nonfinite/domain failure, identity drift, parity failure, invalid terminal transition, containment uncertainty, or resource exhaustion leaves the last admitted parent unchanged and produces only a typed blocked/failure state.

## Hard gates before implementation

D-01–D-07 must separately fix: the qualified verified backend; exact vacuum/trace/table-knot physics; norm/dense reconstruction/defect/stability theorem; QoIs and budgets; all-root/grazing/simultaneous policy; authoritative rec_bianchi identity plus full numerical ABI/parity predicate; and exact successor stage/dependency/resource/worktree authority.

Until then, every P-00–P-16 implementation check remains `NOT_RUN`. No claim of implementation readiness, correctness, performance, historical replay, parity, corrected-reference validity, or scientific promotion is made.

## Independent review and correction

The single independent verdict was `PARTIALLY_CONFIRMED`. It supported H2 as the best research direction and required the successor-stage, conditional-readiness, admission/FSM, #18/#19, and ABI/review-custody corrections. Those were incorporated in one repair/closeout round. `DESIGN_REVIEW.md` binds the exact pre-repair hashes; no second review or inherited final verdict is claimed.

## Source reconciliation

- Rump's verified-numerics work and accurate-summation literature support outward enclosure and rigorous reduction as design obligations, not as proof of this code.
- Estep and the defect/event literature support separating endpoint discrepancy from continuous defect/global/QoI/event-time error, subject to theorem hypotheses.
- SciPy 1.17 documents sign-change event detection and warns that multiple crossings within one step can be missed; its `success` status is not an application admission predicate.
- Python 3.12 documents pipe capture/communication semantics. The current issue is pre-check memory buffering, not a claim that `subprocess.run(capture_output=True)` itself uses the unsafe manual-`wait()` pattern.

## Custody and decision authority

The target HEAD and tracked diff are unchanged. The only status entries remain the same two pre-existing untracked `.bundle` paths, which were not opened, hashed, moved, or modified. Production/history/parity/package/BDF execution was not performed.

This package is reviewer-ready research evidence and a conditional architecture. A new explicit authorization is required to resolve D-01–D-07 or implement any N slice.
