# NEGATIVE RESULTS

## N-001 — Physics-only universal cure

ATTEMPT: Map every prior ODE blocker to a physics-specific remedy.

WHY_IT_WAS_TRIED: The user requested a physics-seeded research loop and an omission-free blocker treatment.

RESULT: Rejected as a universal framing. Eight of 32 families are software/provenance/resource obligations; fourteen more require independent mathematical or software machinery in addition to physics.

WHY_IT_FAILED: Physical invariants cannot authenticate executables, bound pipes, define terminal resume semantics, contain symlinks/packages, or change algorithmic complexity.

WHAT_IT_RULES_OUT: Any “complete physics cure” claim that drops the `S` residuals in `work/ISSUE_SEED_COVERAGE.md`.

WHETHER_TO_REVISIT: No, unless the project scope itself changes.

## N-002 — Positivity/conservation implies exact-zero safety

ATTEMPT: Use the existing MPRK/PDS seed as sufficient justification for closed-cone boundary handling.

WHY_IT_WAS_TRIED: H/He inventories form natural nonnegative simplices.

RESULT: Rejected. Standard MPRK theorems impose strict positivity and denominator/order hypotheses; primary literature documents sticking, oscillation, and order reduction near zero. A nominal-zero published test was corrected to `10^-10`.

WHY_IT_FAILED: An interior positivity theorem is not an invariant-face theorem, and an implicit inverse can populate connected zero components.

WHAT_IT_RULES_OUT: Replacing exact zeros by `realmin`/floors, or declaring the current strict chart boundary-safe because it uses MPRK.

WHETHER_TO_REVISIT: Only as a separately derived active-face/reduced-system method with exact face invariance and nonautonomous order proofs.

## N-003 — Same-path conservation ledgers as validation

ATTEMPT: Use current photon and energy zero-residual checks as independent physical admission.

WHY_IT_WAS_TRIED: The reported residuals include zero.

RESULT: Rejected. Photon assignment is reconstructed from the same total, and unresolved energy is defined as the negative of the later sum.

WHY_IT_FAILED: Common-mode omissions cancel algebraically.

WHAT_IT_RULES_OUT: Scientific validation from these ledgers without primitive-flux reassembly or an independent representation.

WHETHER_TO_REVISIT: After independent source/absorption/escape and thermal-reservoir accumulation is implemented.

## N-004 — Full/two-half discrepancy as LTE/global/QoI authority

ATTEMPT: Treat the current endpoint distance as local truncation error and extend it to trajectory/QoI claims.

WHY_IT_WAS_TRIED: Step doubling is a common diagnostic.

RESULT: Rejected. Both paths use the same map and algebra; no branch-correct reconstruction, continuous defect, stability propagation, adjoint, or QoI budget is active.

WHY_IT_FAILED: Correlated methods can agree while sharing the same model/discretization error, and endpoint discrepancy alone supplies neither a consistency proof nor global stability weighting.

WHAT_IT_RULES_OUT: Renaming the existing metric as LTE or using it alone for scientific admission.

WHETHER_TO_REVISIT: Retain only as one diagnostic alongside a proved estimator.

## N-005 — Existing event and low-rank files imply production closure

ATTEMPT: Credit the repository's event-localization/restart audit and low-rank/JVP modules as current solver capabilities.

WHY_IT_WAS_TRIED: Relevant implementations exist in tracked history/stages.

RESULT: Rejected. Mechanical caller tracing found them disconnected from the active worker chain.

WHY_IT_FAILED: Static code and receipts have no operational effect without an authenticated call path.

WHAT_IT_RULES_OUT: “Already implemented” or performance/event-completeness claims based on file presence.

WHETHER_TO_REVISIT: After active-chain wiring and end-to-end verification.

## N-006 — Counting four denominators as rank four

ATTEMPT: Replace the prior rank-`<=3` seed by a rank-`<=4` ceiling because four group denominators generate four rank-one terms.

WHY_IT_WAS_TRIED: A first symbolic pass counted groupwise outer products rather than their shared factor space.

RESULT: Rejected by independent audit. The right factors of all four terms lie in the span of the three global absorber sums, so the fixed-branch nonlocal contribution has rank at most three.

WHY_IT_FAILED: Term count was confused with matrix rank; shared right factors reduce the span.

WHAT_IT_RULES_OUT: Using four denominators alone to contradict rank `<=3`. It does not establish conditioning, interval factorization, or speedup.

WHETHER_TO_REVISIT: Verify the rank-`<=3` factorization branch by branch and measure numerical rank/conditioning before implementation.

## N-007 — Heuristic interval padding as proof

ATTEMPT: Regard long-double accumulation plus one `nextafter`, or relative padding, as general outward rounding.

WHY_IT_WAS_TRIED: It can widen many ordinary floating-point results cheaply.

RESULT: Falsified. `[1e20,1,-1e20]` excludes exact sum 1, and an exact 2×2 conservative M-matrix witness receives `certified=True` while both published solution intervals exclude the exact coordinates.

WHY_IT_FAILED: Rounding error can exceed one ulp of the rounded result; dependency and inverse/residual errors are not enclosed by cosmetic endpoint widening.

WHAT_IT_RULES_OUT: Current universal interval/Krawczyk containment claims.

WHETHER_TO_REVISIT: Only after qualified outward-rounded primitives, verified elementary functions, and whole-box derivative inclusion are used.

## N-008 — Endpoint-sign event monitoring as all-root logic

ATTEMPT: Use endpoint/hull crossing alone for all table events.

WHY_IT_WAS_TRIED: It is cheap and catches a simple sign-changing crossing.

RESULT: Rejected for all-root claims. Multiple even crossings, grazing/double roots, simultaneous surfaces, and repeated restart events need separate treatment; checked root-isolation literature itself excludes or weakens multiplicity guarantees.

WHY_IT_FAILED: No endpoint sign change is required at an even-multiplicity touch or two crossings, and interpolation-polynomial root completeness is not true-solution completeness without dense-output error.

WHAT_IT_RULES_OUT: Event-completeness or topology-preservation claims from the current active detector.

WHETHER_TO_REVISIT: With all-simple-root isolation, grazing falsifiers, enclosed dense output, and explicit simultaneous-event priority.

## N-009 — Legacy/same-code parity as independent oracle

ATTEMPT: Use historical BDF expected output or guide-invoked same-code parity as independent numerical/physical validation.

WHY_IT_WAS_TRIED: They reproduce known artifacts.

RESULT: Rejected as an oracle, retained as forensic compatibility evidence.

WHY_IT_FAILED: Shared code/data/algebra permit common-mode errors; current BDF products are upstream immutable inputs, and the parity validator is not an authenticated controller transition.

WHAT_IT_RULES_OUT: Corrected-science promotion from replay/parity alone.

WHETHER_TO_REVISIT: Preserve in a compatibility lane; add manufactured/analytic/limiting and separately implemented references in a corrected lane.

## N-010 — Execute despite runtime mismatch

ATTEMPT: Consider running a new current-stage scientific trajectory during the loop.

WHY_IT_WAS_TRIED: Execution could produce performance and endpoint data.

RESULT: Not attempted. Live NumPy and pandas violate the pinned runtime contract.

WHY_IT_FAILED: Such output would be inadmissible by the project's own environment identity rule and would not cure missing independent oracles.

WHAT_IT_RULES_OUT: Any new scientific/performance conclusion from this session.

WHETHER_TO_REVISIT: Only in an exact qualified runtime under separately authorized execution.
