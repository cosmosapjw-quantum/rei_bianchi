# INTEGRATED ODE RESEARCH RECORD

## Immutable preregistration — 2026-08-23

### Question

Can the four preceding ODE research loops be reconciled into one external-audit-ready conclusion and a bounded, owner-neutral successor implementation that converts the confirmed arithmetic/certificate/opacity/admission/FSM false-success surfaces into verified or explicit fail-closed outcomes, without mutating forensic stages or inventing unresolved scientific policy?

### Instruction and authority boundary

- The user now authorizes actual code changes and concrete non-production execution for an external-audit report.
- Attached harness/archive documents govern method only; they do not expand scientific truth, select conventions, or authorize production/replay/promotion.
- Existing A/P/V/C/S/R2A/U/B stages, historical data, parity references, receipts, baselines, tolerances, and both untracked bundles are immutable.
- Implementation is additive under one new N successor stage in an isolated worktree. It is not routed into the active history driver in this work unit.
- Unresolved physics/numerical decisions return typed `UNRESOLVED`/`UNAVAILABLE`/`NOT_CERTIFIED`; they are never filled by a silent default.

### Four evidence sets to integrate

L1 — exhaustive current issue/blocker inventory and active-call-graph audit;
L2 — physics-specific remediation analysis;
L3 — independent mathematics/algorithm/coding remediation analysis;
L4 — the two supplied harness executions as one harness-validation family: physics-harness outcome plus coding-harness outcome/review.

If provenance shows a different natural partition, record it as a deviation without changing the accepted evidence universe.

### Hypotheses

H1 — all frozen blockers can be fully resolved and scientifically admitted now without owner decisions.

- Prediction: no unresolved convention, theorem, backend, reference-lineage, event-policy, runtime-identity, or production gate remains after implementation.
- Falsifier: any D-gate remains owner-dependent or any required production/reference execution stays unauthorized.

H2 — a bounded additive successor can implement and validate the owner-neutral subset while explicitly retaining the remaining blockers and claim ceilings.

- Prediction: fresh predecessor witnesses fail, successor tests pass for exact arithmetic, exact linear certificates, opacity/share helpers, admission, and FSM; no predecessor bytes change; all remaining items are typed and mapped.
- Falsifier: a successor test passes only by relaxing a tolerance, copying the implementation into its oracle, mutating forensic data, or treating unresolved policy as success.

H3 — no safe, useful code change is possible before every D-gate is resolved.

- Prediction: even owner-neutral fail-closed primitives necessarily change active conventions or history.
- Falsifier: an isolated additive module demonstrates useful verified/fail-closed behavior without active routing or historical mutation.

### Planned confirmatory methods

| ID | Method | Close condition |
|---|---|---|
| M-01 | Bind original repository identity, locate and hash the four loop outputs, and construct a provenance/conflict map. | Four evidence sets identified or explicit missing-artifact gaps recorded. |
| M-02 | Create isolated successor worktree and run the documented broad safe baseline. | Exact command/exit/output captured; baseline failure stops implementation unless clearly pre-existing and authorized to continue. |
| M-03 | Run exact predecessor red witnesses for signed cancellation, two known linear certificate exclusions, HeII normalization, terminal FSM, and incomplete admission. | Each result captured before successor code exists; non-reproduced witness is retained as a deviation. |
| M-04 | Implement the frozen owner-neutral successor modules and tests in dependency order. | Only declared N files change; no silent policy/backend default. |
| M-05 | Run focused successor tests, mutation-like negative cases, neighboring safe tests, and deterministic repeat. | Exact command/run data captured; no open confirmatory experiment. |
| M-06 | Integrate findings, full diff, claim ledger, validation ledger, 32-item disposition, and run-data manifest into the final report. | Every strong claim references code, command, artifact, derivation, or source. |
| M-07 | Obtain one independent non-editing review over exact preimage hashes, apply at most one repair-closeout, and reverify custody. | Verdict is confirmed/partially-confirmed/rejected; not inconclusive. |

### Documentation topics — maximum three

T-01 verified interval arithmetic, exact/reproducible summation, and certificate inclusion;
T-02 ODE defect/global/QoI and all-root event semantics;
T-03 SciPy 1.17 and Python 3.12 runtime/process API semantics.

### Stopping rules

- One implementation candidate and the exact frozen module set only.
- At most three external documentation topics, four active agents, one independent review, one repair-closeout, one retry of the same failure, and this one metacognitive pass.
- Stop rather than modify a predecessor, production result, baseline, convention, tolerance, dependency lock, or reference.
- Complete when M-01–M-07 are closed, raw run data and final hashes exist, and original checkout custody is exact; otherwise terminate with the registered bounded decision.

## Append-only evidence and result log

Entries below this line are append-only; the preregistration above is immutable after result collection begins.

### E-001 — repository identity, custody, and isolated worktree

- Confirmatory; methods M-01/M-02.
- Original checkout: `/home/cosmosapjw/Dropbox/bianchi/rei_bianchi/rei_bianchi`.
- Original branch/HEAD: `agent/precalc-adaptive-history-parallel-runtime` / `111b6ace750e36e218df7fc9626c6bad2ec19971`.
- Original tracked diff: empty. Pre-existing untracked custody consists of exactly the two named bundle files in A-12; neither file was read, hashed, moved, modified, packaged, or deleted.
- Isolated linked worktree: `/tmp/rei-ode-integrated-audit.ZcRbz6/worktree`.
- Worktree branch/starting HEAD: `agent/ode-four-loop-audit-hardening-20260823` / `111b6ace750e36e218df7fc9626c6bad2ec19971`.
- Worktree initial status: clean. Git-dir is the repository's linked-worktree git-dir; common-dir remains the original repository common git directory.
- Interpretation: all target writes and non-production validation are confined to this isolated worktree. No commit, push, merge, active routing, or predecessor mutation is authorized.

### E-002 — broad safe baseline and runtime identity

- Confirmatory; method M-02.
- CWD for both commands: `/tmp/rei-ode-integrated-audit.ZcRbz6/worktree`.
- Command:
  `python3 -B -m unittest discover -s stages/Bianchi_Reionization_Stage_P0_5_B2C2B0C_R2C_R1B_R2B_R2A_R2_R1A_R1_R1_R1_R1_R1_R1_UNCERTAINTY_QUALIFIED_FIRST_CANONICAL_INTERVAL_ADAPTIVE_HISTORY/tests -p 'test_*.py'`
- Result: exit `0`; wall time `8.63526495 s`; decisive output: `Ran 49 tests in 8.545s` and `OK`.
- Runtime command:
  `python3 -B -c "import json, platform, sys; import numpy, scipy, pandas; print(json.dumps({'executable': sys.executable, 'implementation': platform.python_implementation(), 'machine': platform.machine(), 'numpy': numpy.__version__, 'pandas': pandas.__version__, 'platform': platform.platform(), 'python': platform.python_version(), 'scipy': scipy.__version__}, sort_keys=True))"`
- Result: exit `0`; wall time `0.18607606 s`; exact stdout:
  `{"executable": "/usr/bin/python3", "implementation": "CPython", "machine": "x86_64", "numpy": "2.4.2", "pandas": "3.0.0", "platform": "Linux-7.0.0-29-generic-x86_64-with-glibc2.39", "python": "3.12.3", "scipy": "1.17.0"}`
- Interpretation: the neighboring 49-test suite is green in this observed environment. The environment is dependency-pin mismatched and therefore this is a local diagnostic baseline only, not historical parity, reference authority, production admission, or scientific validation.

### E-003 — first predecessor numerical probe; instrumentation deviation retained

- Confirmatory; method M-03; exit `0`; wall time `0.003924081 s`.
- Signed-sum result reproduced: input binary64 hex values `[0x1.5af1d78b58c40p+66, 0x1.0000000000000p+0, -0x1.5af1d78b58c40p+66]`, exact rational sum `1/1`, returned interval `[-0x0.0000000000001p-1022, 0x0.0000000000001p-1022]`, `contains_exact=False`.
- Pure-HeII algebraic result reproduced under the declared per-H number-density comparison: `YHE=0.079`, current double-factor value `0.006241`, one-factor value `0.079`, ratio `0.079`.
- Instrumentation deviation: the first Krawczyk report formed `center +/- radius`, but the actual downstream consumer forms `center +/- krawczyk_radius`. Consequently this first report did not reproduce the preregistered two-coordinate exclusion for either system. This is a probe/reporting error, not a changed scientific calculation; its output is retained and no conclusion is taken from it.
- Action: consume the single allowed same-failure retry and rerun only the Krawczyk reporting expression with the source-exact consumer formula. No further same-failure retry remains.

### E-004 — corrected source-exact predecessor Krawczyk consumer probe

- Confirmatory; method M-03; exit `0`; wall time `0.011973543 s`.
- The only corrected expression is the one visible in the predecessor consumer: `nextafter(center - krawczyk_radius, -inf)` and `nextafter(center + krawczyk_radius, +inf)`.
- Simple system `A=[[2,-1],[-5,3]]`, `b=[1,1]`: `certified=[True]`; consumer lower `[3.9999999999999933, 6.9999999999999885]`; consumer upper `[3.9999999999999996, 6.999999999999999]`; exact `[4/1,7/1]`; containment `[False,False]`.
- M-matrix system `A=[[11,-10000],[-10,10001]]`, `b=[1,2]`: `certified=[True]`; consumer lower `[2.9968035161322564,0.003196483867745482]`; consumer upper `[2.996803516132259,0.003196483867745485]`; exact `[30001/10011,32/10011]`; containment `[False,False]`.
- Adjudication: `REPRODUCED_FALSE_CERTIFICATE` for the bounded helper/consumer contract. This does not establish that either matrix occurs on the active trajectory, but it falsifies universal soundness of `certified=True` as currently consumed.

### E-005 — predecessor terminal-resume behavioral witness

- Confirmatory; method M-03; non-production `_test_mode=True` with the tracked fake worker only; exit `0`; wall time `0.306839858 s`; all files existed only inside a `TemporaryDirectory`.
- First run reached `BLOCKED_TABLE_EVENT` with `attempted_endpoints=1`, `transition_number=1`.
- Resume restored `status=BLOCKED_TABLE_EVENT`, attempts `1`, transition `1`, then `run(max_accepted=1)` executed another attempt and published attempts `2`, transition `2`, status `BLOCKED_TABLE_EVENT`.
- Exact probe conclusion: `terminal_was_absorbing=False`.
- Adjudication: `REPRODUCED_TERMINAL_FSM_GAP`. This bounded fake-worker execution demonstrates the control-flow behavior without running a production history, parity, package, or BDF operation.

### E-006 — predecessor incomplete-admission schema witness

- Confirmatory; method M-03; directly exercised the tracked `test_policy.py` PASS-envelope constructor and `validate_and_decide`; exit `0`; wall time `0.000006653 s`.
- Three current-schema rows with `scientific_accept=True` produced `decision=ACCEPT`.
- The accepted row contains the current schema's identity, width, same-lineage ledger, candidate, table-event and `map_enclosed` diagnostics, but it has no explicit `solver_outcome`, `residual_bound`, `expected_terminal`, `event_set_complete`, `source_tree_sha256`, or `diagnostics_complete` predicate.
- Adjudication: `REPRODUCED_ADMISSION_CEILING`, not proof that the accepted fixture is physically wrong. It shows the current admission language can accept without the preregistered mandatory successor evidence, so schema-v1 `ACCEPT` cannot by itself imply the stronger scientific admission claim.

### E-007 — four-loop provenance and bounded completeness adjudication

- Confirmatory; method M-01. Full 370-line record: `FOUR_LOOP_PROVENANCE.md`, SHA-256 `5aa0b98981948cfd2ed460cce4ae4afcf844b321016c56b84ff530e0ad437470` at creation; an apply-patch copy is delivered under N.
- Natural partition: R1 physics and R2 math/algorithm/coding survive only as unverified seeds whose standalone current-REI originals were not located; R3 `/tmp/rei-ode-physics-loop.U8P8Z6` and R4 `/tmp/rei-ode-coding-loop.4qpipi` are the two complete exact-HEAD harness evidence sets.
- Mechanical inventory: 32/32 frozen IDs exactly once, `P=10`, `A=14`, `S=8`; corrected interpretation is 31 correctness/custody obligations plus #18 as one unbenchmarked performance opportunity.
- Wrong-root exclusion: similarly named `/tmp/rec_bianchi_*` files bind a different repository, HEAD, tree, model, and 56-row universe and are not REI evidence.
- Superseding corrections: the signed-sum input is `[1e20,1,-1e20]` and its returned hull is approximately `[-5e-324,5e-324]`; #18 is not a correctness blocker; #19 is `PARTIAL_NARROWED`; R3 promotion is design-tier only and R4 ends `HOLD`; the R4 review binds the pre-repair image only.
- Completeness ceiling: 32/32 is bounded-input bookkeeping, not proof that no undiscovered defect exists and not proof of production reachability or closure.

### E-008 — bounded external-source refresh, three preregistered topics only

- Confirmatory/specification reconciliation; method M-03; retrieved 2026-08-23.
- T-01: Rump, *Verification methods: rigorous results using floating-point arithmetic*, Acta Numerica 19 (2010), DOI `10.1017/S096249291000005X`; primary-author PDF metadata and institutional publication record confirmed title, venue, pages, DOI, and rigorous-computation scope. Applicability: supports theorem-matched verified arithmetic; it does not validate this implementation.
- T-02: official SciPy 1.17.0 `solve_ivp` documentation, `https://docs.scipy.org/doc/scipy-1.17.0/reference/generated/scipy.integrate.solve_ivp.html`. It states event search uses sign changes over steps and multiple crossings in one step can be missed; `success` means interval-end or terminal-event algorithmic completion. Applicability: supports retaining event completeness and application admission as separate obligations.
- T-03: official Python 3.12.13 `subprocess` documentation, `https://docs.python.org/3.12/library/subprocess.html`. It states `communicate()` buffers data in memory and is unsuitable for large/unlimited output, and recommends fully qualified executable paths. Applicability: supports bounded incremental audit capture and tool identity; it does not prove an exploit in the predecessor.
- Search scope stopped at the frozen three topics. No literature statement is treated as a substitute for an executed test, exact witness, or current-source binding.

### E-009 — independent operational localization subtask and dead end

- Supporting confirmatory evidence; read-only subtask at the same HEAD. The agent first ran a pure monkeypatch probe whose dynamic loader failed with exit `1`, `AttributeError: 'NoneType' object has no attribute '__dict__'`, because the module was not registered in `sys.modules` before executing dataclass definitions. This dead end is retained; no scientific result came from it.
- After adding only `sys.modules[name]=module` to the probe loader, the successful non-production command `PYTHONDONTWRITEBYTECODE=1 python3 -B -` completed in approximately `0.2 s`. It used `Coordinator.__new__`, monkeypatched `_attempt/_receipt/_finish` and a no-filesystem temporary-directory sentinel; no constructor, worker, history, parity, package, or BDF path ran.
- Exact stdout:
  `TERMINAL_REENTRY before=BLOCKED_TABLE_EVENT after=BLOCKED_TRANSPORT attempts=1`
  `COMPLETE_REFINISH before=COMPLETE_UNSEALED after=COMPLETE_UNSEALED finish_calls=['COMPLETE_UNSEALED']`
  `INCOMPLETE_POLICY_ADMISSION action=ACCEPT missing=telemetry,global_error_bound,qoi_error_bound,event_completeness all_missing=True`
- Supporting focused command in the subtask: `PYTHONDONTWRITEBYTECODE=1 /usr/bin/time -f 'WALL_SECONDS=%e EXIT_STATUS=%x MAX_RSS_KB=%M' python3 -B -m unittest -v test_policy test_worker_protocol test_state_io`; result `Ran 14 tests in 0.032s`, `OK`, wall `0.22 s`, exit `0`, max RSS `38016 KiB`.
- Interpretation: this independently corroborates E-005/E-006 and additionally shows completed-cursor duplicate finish. It is supporting localization, not a second production exploit. The registered confirmatory same-failure retry remains the source-exact Krawczyk reporting correction in E-003/E-004; this separate loader dead end is not used to launder or replace any failed numerical outcome.

### E-010 — numerical/physics successor red-green-refactor record

- Confirmatory implementation; method M-04. The numerical subtask was confined to N and added `verified_backend.py`, `certificate_adapter.py`, `corrected_physics.py`, one independent integer-pair oracle, and three focused test modules. It did not edit, import-route, or overwrite any predecessor.
- Preimplementation red command: `python3 -B -m unittest -v test_verified_backend_exact.py test_point_linear_certificate.py test_corrected_physics.py`; result exit `1`, three `ModuleNotFoundError` import errors for the intentionally absent successor modules, approximately `0.04 s`. This is the expected structural red, not a scientific failure.
- First green: the same three-module command ran 18 tests, exit `0`, approximately `0.08 s`. Refactor green: 18 tests, exit `0`, approximately `0.08 s`.
- Final mutation-strengthened agent run: `/usr/bin/time -p python3 -B -m unittest -v test_verified_backend_exact.py test_point_linear_certificate.py test_corrected_physics.py`; result exit `0`, `Ran 22 tests in 0.012s`, `OK`, `real 0.07`, `user 0.06`, `sys 0.00`.
- The additional cases perturb determinant/digest/enclosure witnesses, exercise nonfinite certificate and physics inputs, and check owner permutation. Final module SHA-256: `verified_backend.py=4ea3c587a534728890b971422ffffb2e2e698801e5857afcecae86a646b79d34`; `certificate_adapter.py=43df358987a4583625152cf4647d27cf1d3e236f13ec9a089b52d4cffa45802f`; `corrected_physics.py=a7469d650e9fa40f25e347f6a94ed66728dc3cca0c53cb6d8b90abd35c129b42`; independent oracle `c6ff9d059bf48a51017efa8e60fa44c505114a812cd7fdcaf06b2c5fe2c962b3`.
- Implemented claim ceiling: exact finite rational algebra with outward binary64 enclosures; exact point-system certificates only for dimension `1..3`; explicit refusal of nonpoint intervals and unbound transcendental operations; explicit per-H opacity and direct nonnegative shares. This is not a general verified ODE backend, an interval Krawczyk repair, or an active trajectory validation.

### E-011 — operational successor red-green and root review

- Confirmatory implementation; method M-04. The operational subtask was confined to N and added a closed admission contract, pure total terminal FSM, bounded audit capture runner, and three focused test modules.
- Preimplementation behavioral red: 15 assertions exercised the intended closed contracts against absent/stub behavior; exit `1`, approximately `0.05 s`, max RSS `20264 KiB`.
- First green encountered three closed-file errors in audit-capture result construction; exit `1`, approximately `0.54 s`, max RSS `21004 KiB`. This implementation dead end is retained. The fix captured descriptors/state before closing streams; the next run passed all 15 tests in `0.531 s`, wall approximately `0.60 s`, max RSS `22924 KiB`.
- Final module SHA-256: `admission_contract.py=e276751536f16e596820fd7e5cb9eba44331b20344abe37b5bb7cc7c25e1b2df`; `terminal_fsm.py=0497a58e31cb77d10894de737cdb8926b145f39f2a7aba6c0bcd1cc0b00c3504`; `capture_audit_run.py=5b32d4921b1d660c1185e5b945346e984dea2332c9bac1bd59029e47ddfea04a`.
- Root source review confirmed: only `SOLVED` plus exactly one authoritative PASS for each of ten closed gates can admit; worker self-success is data but has no authority; all terminal outcomes are absorbing before action decoding; audit capture uses shell-free resolved executable identity, a fresh exact allowlist environment, bounded incremental binary capture, time/output termination, Git/runtime identity, and raw hashes.
- Explicit negative claim: the runner prelaunch-scans secret-like argv and inherits zero environment variables, but it does not scan captured stdout/stderr for secrets. No report may claim output-secret detection.

### E-012 — confirmatory-capture failures and the sole repair-closeout

- Confirmatory infrastructure; methods M-04/M-05. Audit run `001_predecessor_red` is preserved with manifest SHA-256 `eda5f81019264afeea422795f5548859729699c00d8509700244e735353b8376`. The capture runner succeeded, but the child exited `1` after `151454890 ns`: its fresh environment set `PYTHONNOUSERSITE=1`, while this checkout's NumPy/SciPy/Pandas are installed under `/home/cosmosapjw/.local/lib/python3.12/site-packages`. Exact stderr SHA-256 is `0353b819460d9cdfbe92fde1af6cea8fc6576ac8e6dd24a77471e7003a983bea`; the failure is `ModuleNotFoundError: No module named 'numpy'` before any red vector executed.
- The sole repair-closeout added an explicit `--pythonpath-root` capability. Each root must be absolute, already resolved, a directory, path-separator-free, unique, and one of at most four; it is put into an otherwise fresh environment and manifested with device/inode/mode/direct-entry listing hash. The manifest explicitly labels this as path/direct-listing identity, not a recursive content seal. No ambient environment is inherited.
- Audit run `002_successor_focused` is also preserved with manifest SHA-256 `567b414bee67fdd48c3dfb5d95fb27880309e347833211f52f3873b9d2d2b2a3`. It executed 38 test methods, of which the displayed 37 dots passed, then exited `1` after `715681436 ns` because `test_shadow_characterization.setUpClass` used an N probe constant containing one extra `R1` in the C-stage path. Exact stderr SHA-256 is `da0f41feb85b03c102b5181d0bddb2e64c1d9f6b11f09ea8a6b52b599a8df095`.
- The path constant was corrected to the existing tracked C stage inside the same sole repair-closeout. The failed exact command is not rerun: the global same-failure retry was already consumed by E-003/E-004, so a green rerun would violate the frozen hard maximum. Later evidence may test disjoint commands and verify the corrected path/hash, but may not relabel this failed run as green.
- Interpretation: these are material execution-provenance findings, not discarded setup noise. They lower the final validation claim: the combined all-N discovery command remains a preserved failure even if every independently rerunnable, disjoint successor vector and neighboring suite later passes.

### E-013 — fresh-environment neighboring-suite failure

- Confirmatory; method M-05. Captured run `003_neighbor_49`, manifest SHA-256 `a78d677d02db21fb22591dc36b15f96e9dd643118a6f2487a4aec559c9b3e2a5`, executed all 49 neighboring tests in `8677387798 ns`.
- Result: exit `1`; 46 tests passed and three `test_launcher` methods errored before their subprocess operation because the test helper indexed `environment['PATH']`, while the first repaired capture environment still omitted PATH. Exact stderr SHA-256: `60870b7a820d8878efdc34c2b514cc780f8bab05bc3530e84771289839c5206a`.
- The open sole repair-closeout added fixed, public and manifested `PATH=/usr/bin`. The exact 49-test command was not rerun. The preimplementation ambient-shell 49/49 green remains a separate baseline, not a replacement for this failed fresh-environment result.

### E-014 — disjoint post-repair green cells and deterministic vector

- Run 004, audit capture tests: manifest `9566e07ee9f3f8de09f5f1eb656a5c39650ee734a6ad6d0f4cac7df7e4fef1c5`; exit `0`; duration `617249747 ns`; `Ran 5 tests in 0.560s`, `OK`.
- Run 005, numerical/physics tests: manifest `b2bc3c1a891bf5181a7a73b88275d27c312b8ab3cf05139b9f270a481b12b756`; exit `0`; duration `101266094 ns`; `Ran 22 tests in 0.021s`, `OK`.
- Run 006, admission/FSM tests: manifest `05cf314e53bec19b8af758cc82cd26410a8472177e84ce7aba6ba861103e3625`; exit `0`; duration `68421501 ns`; `Ran 11 tests in 0.005s`, `OK`.
- Runs 007/008, successor integrated vector: exits `0`; durations `55965249` and `55563184 ns`; both exact stdout streams are 2,824 bytes with SHA-256 `e353ab05f9158e319f69d3251a890ee29ca587fbcf194654a74f1c7ccac13b1b`.
- The deterministic vector establishes exact sum `1`, exact independent replay of both 2x2 systems, the one-factor per-H opacity fixture, direct shares, typed zero-opacity inconsistency, missing-gate block and byte-identical terminal absorption. It does not run an ODE trajectory.
- Before the successful run 004, one outer command used a repository-relative runner path while its shell cwd was the N tests directory and failed before creating a capture directory. It is retained in the research narrative as an operator invocation dead end, not a module result.

### E-015 — predecessor custody, runtime and audit-index closure

- Run 009: manifest `62c2672498327de8cdf8f28b54da2c9416e58af2bccb6adadcb72da534faf8f2`; 1,012 declared/checked predecessor files, zero failures, exit `0`, `67637380 ns`.
- Run 010: manifest `1841c1ee6c63e28934b839df26c07fa9d95d29f95a01b3ab8f06d2516626589c`; exit `0`, `250416322 ns`; CPython 3.12.3, NumPy 2.4.2, SciPy 1.17.0, pandas 3.0.0 from the explicit user-site root.
- `AUDIT_RUN_INDEX.json` was independently compared to every manifest, sidecar and raw stream: ten rows/ten declared, zero mismatches. Every stream is uncapped and complete until termination.
- `ADDITIVE_SHA256SUMS.txt` binds 68 pre-review files and has SHA-256 `ba71b4860328d9535ec5709d084176ed72c749264154ae61ff769dc4e5a7f6b0`; `sha256sum -c` returned OK for all 68. Report preimage SHA-256: `26fd6a56fc0dcaab157a6abd63a3906905a5cc42c4d6a362805c5f027c2affa7`.
- Original checkout closeout recheck: same branch/HEAD/tree, no tracked changes, and exactly the same two named untracked bundles. No N bytecode cache exists. Worktree status contains only the additive N directory.

### E-016 — pre-review adjudication

- H1 falsified; H2 confirmed only at owner-neutral local shadow level; H3 falsified.
- Proposed final engineering verdict: `PARTIALLY_CONFIRMED`; promotion `HOLD / FORBIDDEN`; active scientific closure `0/32` because D-01--D-07 remain open.
- The preimage is frozen for one fresh, read-only independent review. No further code repair is available under the hard maximum.

### E-017 — independent review and bounded closeout

- The fresh non-editing reviewer returned exactly `PARTIALLY_CONFIRMED` and supported `HOLD / FORBIDDEN`. Review artifact SHA-256: `b4ee087ad0a576a504e96e60227fdaf5e315422edc6144af596f4a56c3bd4422`.
- Four medium findings: decimal parsing and integer power can consume resources before the declared exact ceiling check; an escaped new-session descendant can retain capture pipes after the original process group is killed; per-run manifests do not hash the untracked recorder source preimage; atomic opacity does not ensure absorber counts fit total inventories. Two low findings: the oracle ignores redundant dimension/residual flags and can raise on some malformed bounds; run-002 narrative said 37 displayed passes although raw output says `Ran 38 tests` with one setUpClass error.
- Independent property checks confirmed 20,006 rational/binary64 enclosures, 15,000 interval exports, 2,000 exact sums, 900 random nonsingular 1x1--3x3 systems, 5,000 opacity and 5,000 share cases, 63 admission mutations, and 300 valid FSM cases. The reviewer did not rerun exact failed commands 001--003; one distinct ambient read-only predecessor probe reproduced the five predecessor surfaces.
- The reviewed report and 68-file preimage hashes remained exact. The review is appended without modifying that preimage. `AUDIT_MANIFEST.json` SHA-256 is `2eb9a4bf2a37e70fac6c1fb00109b47c5d78432e61052e56802de5dde711043e`.
- Because a registered A04 acceptance defect remains after the sole repair-closeout, bounded decision is `STOP_INVALID`, failure class `REGISTERED_ACCEPTANCE_FAILURE`. This does not erase the delivered shadow code/evidence; it means the candidate cannot be accepted as satisfying the frozen implementation contract and remains prohibited from promotion.
- Final contract SHA-256 before mandated ephemeral deletion: `2ae45d16a6436bc139f4c9d265e2b5e18d35af5e84c8f64590e0ac95981c8a7d`; final harness check: PASS.
