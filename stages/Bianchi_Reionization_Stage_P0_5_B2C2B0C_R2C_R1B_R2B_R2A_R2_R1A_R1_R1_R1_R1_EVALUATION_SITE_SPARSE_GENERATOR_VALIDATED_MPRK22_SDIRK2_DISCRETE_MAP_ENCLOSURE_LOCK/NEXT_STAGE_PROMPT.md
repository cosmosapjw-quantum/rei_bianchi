# Next exact stage prompt

Execute:

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-R1-R1-R1-R1-CROSS-SITE-STATE-FEEDBACK-REMAINDER-AND-TABLE-EVENT-LOCK
```

## Inherit without modification

- prerequisite and all inputs locked by the present stage;
- four explicit source evaluation sites;
- 26-event registry, MPRK22(1), Alexander-SDIRK2;
- exact He II Ly-alpha heat and unresolved OTS ledger;
- canonical forcing, owner law and structural-zero masks;
- all primal parity, local certificate and counterexample receipts.

## Required work

1. Keep independent `(v,f,vf)` local generators at all four sites.
2. Differentiate state-dependent owner normalization as local diagonal plus a
   named rank-one reduction for each owner/group/site.
3. Propagate outward second-order terms from population, temperature, branch
   and owner feedback; store each remainder owner separately.
4. Build a validated image that contains the 24 inherited trajectories, the
   upper-to-lower witness and interior adversarial samples.
5. When the temperature image touches `T=10^(4+0.25 k) K`, reject the trial,
   localize the earliest event, preserve parent bytes, rebuild the fixed
   topology and restart.
6. Close H nuclei, He nuclei, group photons, resolved heat, unresolved OTS,
   escaped radiation and total energy as set inclusions.
7. Run all three shape lanes; no post-hoc selection.
8. Authorize the first uncertainty-qualified canonical interval only if every
   inclusion passes and
   `Delta x_HII`, `Delta x_HeII`, `Delta x_HeIII`, `Delta log(T) < 2e-3`.

## Fail-closed categories

```text
CROSS_SITE_REMAINDER_DIVERGENCE
OWNER_LOW_RANK_BOUND_FAILURE
TABLE_EVENT_UNLOCALIZED
SET_LEDGER_EXCLUDES_ZERO
PUBLIC_WIDTH_GATE_FAILURE
TRANSACTION_ROLLBACK_FAILURE
```

Do not begin Bianchi-background coupling, recombination splice, CAMB transfer or
production node chemistry in this stage.
