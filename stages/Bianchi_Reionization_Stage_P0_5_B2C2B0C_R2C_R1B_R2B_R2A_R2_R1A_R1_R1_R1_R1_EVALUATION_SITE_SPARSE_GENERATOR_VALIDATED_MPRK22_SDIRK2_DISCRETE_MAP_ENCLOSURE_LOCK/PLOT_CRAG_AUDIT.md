# Plot-driven CRAG audit

## Plot readings

1. `local_implicit_certificate_margins.png`: every local population and thermal
   contraction lies well below the strict gate `1`; the worst value is
   `0.0311021`.
2. `thermal_root_krawczyk_inclusion.png`: the scalar Krawczyk image occupies at
   most `9.97064e-05` of the chosen `1e-8` log-temperature tube.
3. `stagewise_witness_static_hull_escape.png`: only `x_HeIII` visibly exits the
   inherited static hull, so static control coherence is specifically falsified
   rather than globally inaccurate.
4. `inherited_widths_vs_public_gate.png`: sampled corner widths are all below the
   public gate, but this is not an inclusion theorem.
5. `inherited_table_event_distance.png`: no inherited endpoint envelope touches
   a table knot; the smallest distance is `0.000291564`.

## CRAG

- **Correctness:** the plots reproduce machine-readable receipts; no graphical
  claim contradicts the data.
- **Retrieval:** local Krawczyk success is consistent with interval existence
  tests, but it cannot substitute for a global flow enclosure.
- **Augmented:** the stagewise schedule mutation breaks the static-hull claim
  while all physical hard gates remain closed.
- **Generation:** a valid next image must carry independent site generators and
  an outward composition remainder; otherwise it will miss the same direction.

## Claim status

- **Surviving:** local MPRK and thermal roots are not the present blocker.
- **Narrowed:** the source-safe trajectories appear numerically narrow for this
  microstep.
- **Rejected:** four static corner trajectories certify the continuous family.
- **Rejected:** positive sampled table-knot distance proves no topology event.
