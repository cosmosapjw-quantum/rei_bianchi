# Next exact stage

Execute:

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1-SOURCE-BRANCH-KERNEL-AND-OTS-ENERGY-MOMENT-LOCK
```

1. Keep the promoted event registry, MPRK22(1), Alexander-SDIRK2, material state, owner law, and ten ledgers fixed.
2. Recover or construct a byte/source-locked adapter for the cited temperature-dependent `v(T)` and the source-defined Ly-alpha absorption fraction `f` over the canonical state domain.
3. Do not extrapolate outside a source table silently. If the source does not define the required range, declare a bounded model family and propagate an uncertainty lane.
4. Lock species- and packet-resolved OTS excess-energy moments as functions of packet class and optical depth; assign each once to resolved heat, unresolved OTS radiation, or escape.
5. Require population, photon number, and total energy parity against the promoted event registry.
6. Preserve the hydrogen-frame Bianchi thermal work term `p Theta_H V` and the covariant Thomson optical-depth adapter, but do not implement CAMB/Bianchi feedback yet.
7. Authorize `FIRST-CANONICAL-INTERVAL-ADAPTIVE-HISTORY` only if source branch kernels and energy moments close over the full 46,080-node state range.
8. Continue read-only monitoring of `rec_bianchi`; import no numerical rates, state, or accepted history.
