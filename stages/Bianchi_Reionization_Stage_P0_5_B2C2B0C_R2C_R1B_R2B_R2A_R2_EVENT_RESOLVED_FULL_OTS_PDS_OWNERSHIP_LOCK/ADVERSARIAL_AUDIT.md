# Independent adversarial audit

## Attacks performed

1. **Infer a direct He I to He III event from the helium net RHS.** Rejected: no source channel supports it, and the registry count is exactly zero.
2. **Drop zero-stoichiometry radiative events.** Rejected: population equations would remain unchanged while photon/energy ledgers would fail.
3. **Call the project `v(T)` source-identical.** Rejected: no source table or interpolation lock exists.
4. **Promote the project `f(x_HI)` outside the source table range.** Rejected by 44,904-node counterexample.
5. **Deposit all OTS excess energy as resolved heat.** Rejected: child-packet spectra/optical-depth moments are absent and would double count the existing external heating lane.
6. **Set unknown OTS excess to zero.** Rejected: violates total energy accounting; an unresolved ledger is required.
7. **Use `3H pV` for finite tilt and arbitrary Bianchi type.** Rejected: the covariant work term is `p Theta_H V`.
8. **Use scalar branching to create a directional Bianchi four-force.** Rejected: angular momentum transfer is not identified.

## Surviving claim

The source-conditioned expected event graph and photon-number ownership are identified and exact. Total energy is exact only in the augmented resolved-plus-unresolved ledger. Source-identical branch kernels and fully resolved OTS heating remain open.
