# Attempt 0 — precalculation input-path mismatch

Before any scientific calculation, the intake script selected an obsolete density-map location. The mismatch was detected while assembling the input lock. No owner split, capacity calculation, or numerical result had been produced.

The attempt was stopped, the canonical P0.4 density map was located and hashed, and `INPUT_LOCK_EXTENSION_1.json` was committed before calculation. This is an intake-path failure only; no science result was altered or discarded.
