# Scientific contract

The entire source-safe state-dependent branch family is certified only if a rigorous flow enclosure contains it and remains narrower than `2e-3` in `x_HII`, `x_HeII`, `x_HeIII`, and `log T`.  Numerical corner trajectories do not substitute for this certificate.  A failed enclosure method may block promotion but may not be represented as a physical no-go without an independent existence argument.
