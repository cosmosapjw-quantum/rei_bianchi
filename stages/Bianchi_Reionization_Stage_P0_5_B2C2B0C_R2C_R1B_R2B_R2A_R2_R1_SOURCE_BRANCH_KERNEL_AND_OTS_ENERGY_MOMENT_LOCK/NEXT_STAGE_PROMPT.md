# Next exact stage

Execute:

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-
FOUR-CORNER-BRANCH-AND-UNRESOLVED-OTS-ENERGY-PROPAGATION-PREFLIGHT
```

1. Create a new durable directory, input lock, state, receipts, manifest and `SHA256SUMS` before calculation.
2. Keep the 26-event registry, MPRK22(1), Alexander-SDIRK2, analytic root backend, material state, owner law and ten ledgers fixed.
3. Propagate every predeclared `(v,f)` corner.  Inside the Hummer--Seaton table domain use the convex cell endpoints and the named log-linear adapter as separate lanes; below `10^4 K` retain the strict `v in [0,1]` lanes.
4. Do not select a corner after seeing the result.
5. Resolve He II Ly-alpha heating with the exact `40.813320 eV` packet energy.
6. For two-photon, free-bound, Balmer and case-B packets propagate the locked first-moment bounds or keep energy in `E_OTS^unres`; never set unidentified energy to zero.
7. Require population, photon and total-energy closure at every microstep.
8. Record interval enclosures for ionization and temperature.  A narrow central history is not production evidence unless every envelope lane closes and the uncertainty meets a predeclared gate.
9. Fail closed on branch-domain escape, energy-owner duplication, fixed-point failure or thermal-envelope nonclosure.
10. Only after this preflight may a source-extension calibration stage or a first canonical interval uncertainty-qualified history be considered.
