# 08 External gate

A source-identical production branch kernel remains unavailable because:

- 46.875% of canonical nodes lie below the Hummer--Seaton table domain;
- the historical interpolation rule is unspecified;
- `f(x_HI)` is not supplied, only `0.1 <= f <= 1`;
- free-bound, Balmer-continuum and two-photon first energy moments are not fully identified.

The stage therefore authorizes only a bounded branch/energy propagation preflight.
