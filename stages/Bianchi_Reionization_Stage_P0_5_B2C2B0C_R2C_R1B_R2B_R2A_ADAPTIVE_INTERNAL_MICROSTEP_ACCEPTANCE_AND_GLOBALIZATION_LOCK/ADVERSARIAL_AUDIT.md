# Adversarial audit

1. **Post-hoc timestep rescue.** Partition 4096 passes the local-error gate, but
   the pre-calculation lock fixed 1024 as the minimum. The deeper result is
   retained only as an auditor and cannot promote the stage.
2. **Weighted-tail masking.** The hard maximum-node metric remains load-bearing;
   density- or mass-weighted summaries may not hide the temperature outlier.
3. **Lane selection.** All three lanes were run. No lane was selected after
   seeing results.
4. **Subgrid leakage.** `EFFECTIVE_HI_SUBGRID` has no resolved H, He or thermal
   source path; exact-zero tests and ledger routing enforce this structurally.
5. **Clipping or projection.** None is available in the production code path.
   Infeasible states fail before commit.
6. **False performance claim.** Only the owner hot path has a comparable legacy
   benchmark and is promoted. Whole-solver speedup remains unresolved.
7. **JAX benchmark illusion.** Isolated JIT speed is not accepted as production
   evidence. Science-sequence synchronization stalls force fallback to NumPy.
8. **Recombination contamination.** The changed `rec_bianchi` SHA was reviewed
   read-only; no rate, state, accepted history, directional closure or surrogate
   was imported.
9. **Physical no-go overreach.** Fixed point, positivity, conservation and
   thermal balance pass at small steps. The fail-closed result is numerical
   local-error policy, not physical-history nonexistence.
