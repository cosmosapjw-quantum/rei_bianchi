# Independent adversarial review

## Strongest objections

1. **The original capacity inequality was not arbitrary.** Correct.  As a
   whole-interval necessary bound it remains useful and is retained in
   cumulative form.  The rejected step is its promotion to a node-local state
   and its independent equilibrium extrapolation.
2. **The endpoint-local budget reconstruction uses endpoint rather than
   time-averaged neutral fractions.** Correct.  Its large mismatch is only a
   diagnostic that the old mass-proportional allocation is not locally
   derived.  The decisive argument is the exact `Delta t` dependence and the
   original code definition.
3. **A radiation photon density could be a state.** Correct in a full RT
   system.  The R2B quantity under audit is `J_sink_node`, an absorption event
   current, not `N_gamma,g`.  A future radiation-store extension must introduce
   and ledger that variable explicitly rather than relabel `J_g`.
4. **A convex endpoint segment need not satisfy the chemistry ODE.** Correct.
   It is a counterexample to a claimed cone obstruction, not the production
   history.  R2C-R1B must construct the nonautonomous forcing and cumulative
   ledger.
5. **Temperature is not closed by the H positivity proof.** Correct.  The
   thermal equation and optical-depth-dependent convergence gate remain
   independent blockers.
6. **Helium and recombination radiation could change the generator.** Correct.
   This stage inherits the H-primary G1/G2a lane and structural exact zeros.
   It does not substitute for the external `rec_bianchi` adapter or full H/He
   production closure.

## Reviewer verdict

`PROMOTE H2`, `REJECT H1`, `HOLD H3`.

The evidence is sufficient to remove the *physical-no-go interpretation* of
all 497 certificates.  It is insufficient to authorize production history.
The smallest next test is a nonautonomous, photon-conserving cumulative-budget
forcing lock with an independent thermal convergence gate.
