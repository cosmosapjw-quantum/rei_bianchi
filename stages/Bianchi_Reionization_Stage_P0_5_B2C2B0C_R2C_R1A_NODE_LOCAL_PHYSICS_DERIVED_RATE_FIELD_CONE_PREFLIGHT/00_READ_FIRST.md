# R2C-R1A state–flux–budget reclassification — read first

Current durable stage:

```text
P0.5-B2C2B0C-R2C-R1A-NODE-LOCAL-PHYSICS-DERIVED-RATE-FIELD-CONE-PREFLIGHT
DURABLE_PASS_R2C_R1A_STATE_FLUX_BUDGET_RECLASSIFICATION_RESOLVES_FARKAS_BLOCKER_R1B_AUTHORIZED
```

This physics-only research stage resolves the R2C-R1 blocker at the level at
which it was actually created. The 497 Farkas certificates remain valid
certificates against the macro-shared common-equilibrium surrogate. They are
not physical-history no-go theorems.

The decisive correction is a state-space reclassification:

- material states are `M=N_HI+N_HII`, `N_HI`, `N_HII`, and a thermal audit
  coordinate;
- `J_g`, `Gamma_g`, `kappa_g`, and `Phi_g` are radiation-coupled algebraic
  rates/fluxes, not independently relaxing material stores;
- `C_Delta t=N_HI,start/Delta t+R_rec,average` is a whole-interval
  storage-plus-recombination budget rate. It changes when the bookkeeping
  interval is refined and therefore is not an autonomous state;
- the physical H subsystem is generated by a positive, H-nucleus-conserving
  two-state reaction operator, while absorbed photons close through a
  cumulative ledger.

The all-row audit covered 1,382,400 inherited node states, 2,764,800 active
photon-group rows, 540 macro cases, and all three shape lanes. It found no
endpoint state-cone, current-sign, opacity-sign, finite-rate, or current–Gamma
failure. All 540 endpoint pairs lie in a convex state/mass-cap segment. The
six nominal mass-cap Farkas cases have both endpoints and their interpolated
cap segment inside the admissible cone.

This stage does **not** provide a unique interior radiation or temperature
history and does not authorize production node chemistry. The next bounded
stage is `P0.5-B2C2B0C-R2C-R1B-PHOTON-CONSERVING-CUMULATIVE-BUDGET-NONAUTONOMOUS-RT-FORCING-LOCK`. It must remove `C` from the dynamical state, construct
nonautonomous photon-conserving forcing, and close the cumulative photon/H/He
ledgers under time refinement before any history is promoted.
