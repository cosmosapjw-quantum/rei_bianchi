# Next exact stage

Execute:

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-ADAPTIVE-INTERNAL-MICROSTEP-ACCEPTANCE-AND-GLOBALIZATION-LOCK
```

1. Create the durable directory, input lock, stage state, receipts, manifest and `SHA256SUMS` before calculation.
2. Inherit the R2B-R1 material state/owner law, R2B-R2 operator, forcing and ten ledgers unchanged.
3. Use deterministic recursive bisection on a rejected internal step. Start from the nominal `dt/8` macro partition; do not jump directly to the observed witness size.
4. Prelock `dt_min = interval/1024`, fixed-point tolerance `1e-10`, nuclei/owner tolerance `1e-11`, photon hard gate `1e-8`, and local step-doubling state error `2e-4`.
5. Accept a microstep only when positivity, H/He nuclei, photon owners, thermal balance, fixed-point convergence and step-doubling error all close.
6. Commit accepted material state and ledgers exactly once. Rejection, event rollback and restart must preserve parent bytes.
7. Keep the hard maximum-node residual. Density/mass weighted residuals are auditors only.
8. Run the primary subgrid lane and both historical auditor lanes without post-hoc selection.
9. First close the complete first canonical interval at `dt,dt/2,dt/4,dt/8` macro outputs. Only then authorize a full five-interval history.
10. Fail closed at `dt_min` with the earliest certificate: fixed point, local error, material capacity, thermal, subgrid exchange, or boundary/storage.
11. `rec_bianchi` remains semantics-only; import no numerical recombination state/rate/history and create no surrogate.
12. Do not start unresolved subtraction, front/Q_M, source/fesc fitting, CAMB transfer, or Bianchi feedback.
