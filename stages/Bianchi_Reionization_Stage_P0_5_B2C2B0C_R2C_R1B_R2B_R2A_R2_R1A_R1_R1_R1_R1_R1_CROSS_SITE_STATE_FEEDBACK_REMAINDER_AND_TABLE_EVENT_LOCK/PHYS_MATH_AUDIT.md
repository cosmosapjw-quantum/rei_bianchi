# PHYS-MATH audit

## Conventions

- metric signature: `(-,+,+,+)`;
- `epsilon_123=+1`;
- natural units are not used;
- state fractions and `ln T` are dimensionless;
- table knots are in kelvin.

## Ledger

| Item | Audit result |
|---|---|
| H nuclei | exact stoichiometric invariant |
| He nuclei | exact stoichiometric invariant |
| owner partition | exact simplex identity |
| group photon ownership | exact identity |
| augmented energy | exact identity including unresolved OTS and escape owners |
| positivity | nonnegative event fluxes and certified MPRK images |
| thermal root regularity | derivative intervals exclude zero |
| table topology | path-hull detector plus transactional localizer |
| FLRW limit | inherited primal map reproduced at point-degenerate/lower control |

## Hidden assumptions

1. The source-safe `(v,f)` intervals are those locked in the prerequisite; no
   new temporal correlation is introduced.
2. The binary64 `nextafter` enclosure is the project numerical authority on the
   sealed platform, not a universal arbitrary-BLAS theorem.
3. The certified state is the algorithm-selected implicit root connected to the
   inherited seed; unrelated mathematical roots are outside scope.
4. The current microstep contains no real table event; the event claim is a
   contract/transaction validation on a synthetic crossing.

## Dimensional check

Every public width and local-error block is dimensionless. Event distance is
measured in `ln T`. Reaction rates and time are inherited in reciprocal seconds
and seconds, so every exponent and Patankar coefficient is dimensionless.

## Special-case attacks

- static four-corner coherence: rejected by the stagewise-switch witness;
- endpoint-only event detection: rejected by a between-site crossing;
- thermal secant as derivative: rejected and preserved as an invalid attempt;
- wide construction tube as solution image: rejected and preserved;
- uncertainty width without truncation error: rejected by adding the full-
  versus-two-half-step gate.

## Verdict

No P0 physics or mathematical inconsistency remains for the one-microstep
claim. Long-time composition, real event encounters, and Bianchi-frame work
remain outside scope.
