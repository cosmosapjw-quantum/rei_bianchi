# Next exact execution prompt

Treat this as a durable continuation of `rei_bianchi`.

## Prerequisite

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-R1-R1-R1-R1-CROSS-SITE-STATE-FEEDBACK-REMAINDER-AND-TABLE-EVENT-LOCK
DURABLE_PASS_R2_R1A_R1_R1_R1_R1_R1_FOUR_SITE_MICROSTEP_ENCLOSURE_LOCAL_ERROR_CONTAINMENT_TABLE_RESTART_AND_STRUCTURAL_LEDGER_PASS_FIRST_CANONICAL_INTERVAL_AUTHORIZED
```

Verify the prerequisite commit, stage `SHA256SUMS`, compact artifact, project
state, durable ledger and remote state before calculation. Do not inherit
transcript-only claims.

## Execute

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-R1-R1-R1-R1-R1-UNCERTAINTY-QUALIFIED-FIRST-CANONICAL-INTERVAL-ADAPTIVE-HISTORY
```

### Locked inputs

- 26-event full-OTS registry;
- MPRK22(1) + Alexander SDIRK2;
- four independent source-evaluation sites;
- source-safe branch intervals;
- exact HeII Ly-alpha heating and unresolved OTS/escape owners;
- canonical BDF first interval forcing;
- cross-site microstep enclosure and full-vs-two-half acceptance;
- table-event transactional restart;
- all three shape lanes.

### Exact task

1. Create and commit the new durable stage directory, input lock, stage state,
   receipts skeleton, manifest skeleton and SHA policy before calculation.
2. Start from partition 2048. Compose certified accepted microsteps across the
   complete first canonical BDF interval.
3. For every attempted microstep compute one full image and two half images.
   Accept only if all local implicit, positivity, width, structural-ledger and
   validated local-error gates pass.
4. On failure, bisect only that attempted step. Keep a locked minimum step and
   fail-close if it is reached.
5. Track sparse generator rank, named owner low-rank modes, remainder growth,
   public widths, table-event distance and accepted/rejected attempts.
6. If a Hummer-Seaton knot is touched, reject, localize the earliest crossing,
   preserve parent bytes, rebuild the fixed-topology source cell and restart.
7. Commit accepted history exactly once per accepted endpoint. Rejected
   attempts and event rollbacks must not mutate accepted state or ledgers.
8. Run all three lanes without post-hoc selection.
9. Independently validate containment of endpoint point trajectories and
   restart identity. Sampling may falsify but not certify.
10. Close exact/set-valued H nuclei, He nuclei, group photon, resolved heat,
    unresolved OTS, escaped-radiation and total-energy ledgers across the whole
    interval.

### Promotion gate

Authorize a hydrogen-frame background-adapter stage only if the full first
interval closes in every lane with

\[
\Delta x_{\rm HII},\Delta x_{\rm HeII},\Delta x_{\rm HeIII},\Delta\ln T
<2\times10^{-3},
\]

and no transaction, event, local-error or structural-ledger failure.

### Still forbidden

- production node chemistry or `B2C2B` promotion;
- Bianchi family sweep before the FLRW first interval;
- recombination numerical import or surrogate;
- source/fesc fitting, front/Q_M, CAMB transfer or Bianchi feedback;
- clipping, favorable lane selection, table extrapolation or post-hoc parameter
  correlation.

After the first interval passes, the next order is:

```text
hydrogen-frame background adapter
-> Bianchi I / tilted V / IX-D sentinels
-> conventional eleven-family sweep
```
