# Adversarial review
Attacks include local sign-selected branch fields, raw versus robust endpoint-hull tests, determinant near-degeneracy, table-domain split, and dense-memory inflation.  The endpoint adversaries remain inside after robust tolerance, but the exact rank theorem independently rejects H1.
