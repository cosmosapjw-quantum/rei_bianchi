# Next exact stage

Execute:

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-R1-R1-
SPARSE-LOCAL-GENERATOR-AFFINE-TAYLOR-MODEL-ENCLOSURE-LOCK
```

1. Keep the 26-event registry, MPRK22(1), Alexander-SDIRK2, exact He II Ly-alpha heating, unresolved OTS energy ledger, canonical forcing, owner law, and all previous endpoints fixed.
2. Represent each node's source-safe `v_i` and `f_i` uncertainty with two local first-order generators and one local `v_i f_i` generator; do not collapse them to two global parameters.
3. Preserve block-diagonal local support and store global owner-normalization/forcing effects as separately named low-rank generators.
4. Remove H and He invariant directions analytically and verify exact nuclei identities generator by generator.
5. Localize every Hummer–Seaton table boundary and restart the representation there.  No extrapolation or numerical floor is allowed.
6. Use a validated interval or ellipsoidal remainder for neglected higher-order terms and record generator rank, remainder radius, wrapping growth, and table-event distance per accepted substep.
7. Reproduce the point-degenerate oracle, all 24 prior trajectories, coherent-grid trajectories, and interior node-local falsification samples.
8. Close photon, resolved thermal, unresolved OTS, escaped-energy, and total-energy ledgers as set inclusions.
9. Authorize an uncertainty-qualified first canonical interval only if all three shape lanes are certified and every public endpoint width is below `2e-3`.
10. If sparse generator growth remains uncontrolled, fail closed and authorize a source-correlation/calibration stage; do not impose global coherence post hoc.
