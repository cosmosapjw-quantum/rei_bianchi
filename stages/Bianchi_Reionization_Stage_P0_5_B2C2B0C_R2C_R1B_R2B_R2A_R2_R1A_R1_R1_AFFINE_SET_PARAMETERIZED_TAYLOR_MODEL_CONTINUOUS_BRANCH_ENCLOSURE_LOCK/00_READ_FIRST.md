# Read first — affine/Taylor continuous branch enclosure audit

This stage tests the authorized low-rank affine/Taylor architecture against the actual source-safe branch family.  It closes as a method fail-closed, not a physical no-go.

The source-safe admissible set permits node-local independent values of `v_i` within the Hummer–Seaton source bracket (or `[0,1]` below the table) and `f_i in [0.1,1]`.  A two-global-parameter coherent field is a separate conditional auditor.
