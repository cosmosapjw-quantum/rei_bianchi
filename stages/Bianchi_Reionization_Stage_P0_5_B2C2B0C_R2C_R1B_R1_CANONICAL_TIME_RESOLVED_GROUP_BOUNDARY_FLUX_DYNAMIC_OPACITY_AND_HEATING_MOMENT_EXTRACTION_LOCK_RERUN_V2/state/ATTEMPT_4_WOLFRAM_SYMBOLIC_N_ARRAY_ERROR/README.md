# Failed symbolic attempt

The first Wolfram call used `Array[h,n]` with a symbolic `n`. `Array` requires a concrete machine-sized dimension, so the result was not a valid proof object. No scientific result was accepted from that call. The successful replay fixes the finite illustrative support to three symbols while proving the normalization and moment identities algebraically; the general finite-support formula is stated separately in the formalism.
