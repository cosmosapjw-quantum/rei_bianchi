# Foreground runtime-limit interruption

The first dynamic-opacity all-node run exceeded the foreground tool-call limit before producing a final result. It was not interpreted as a physical or numerical failure. The same locked calculation was rerun as a monitored process and completed; its accepted log is `receipts/DYNAMIC_OPACITY_RUN.log`.
