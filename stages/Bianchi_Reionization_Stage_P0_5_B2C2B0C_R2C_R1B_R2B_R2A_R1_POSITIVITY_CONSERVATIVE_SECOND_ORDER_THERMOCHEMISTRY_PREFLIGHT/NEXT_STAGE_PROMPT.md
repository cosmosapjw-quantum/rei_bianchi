# Next exact stage

Execute:

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-EVENT-RESOLVED-FULL-OTS-PDS-OWNERSHIP-LOCK
```

1. Create the durable directory, input lock, stage state, receipts, manifest and
   `SHA256SUMS` before calculation.
2. Keep the accepted MPRK22(1), Alexander-SDIRK2, analytic-root backend,
   46,080-node material state, owner law and ten-ledger structure fixed.
3. Refactor the source full-OTS operator to emit nonnegative **event-resolved**
   reaction fluxes, not only the summed five-component RHS.
4. Give each photoionization, collisional ionization, recombination and OTS
   cascade term exactly one material and energy owner.  Preserve all fractional
   OTS branching coefficients from the source formulas.
5. Require event-flux reconstruction to reproduce both the original population
   RHS and the original thermal source at source arithmetic precision.
6. Forbid a direct HeI-to-HeIII event unless it exists in the source event
   registry.  Do not infer event topology from the net RHS.
7. Preserve exact H/He nuclei conservation, strict positivity, unsupported
   structural zeros and exact-zero subgrid resolved source.
8. Use Wolfram to verify each stoichiometric vector and the total H/He invariants;
   use independent Decimal/mpmath replay for source-event sums.
9. Only after event-level population and energy parity passes may the next stage
   authorize `FIRST-CANONICAL-INTERVAL-ADAPTIVE-HISTORY`.
10. Continue read-only monitoring of `rec_bianchi`; do not import its rates,
    accepted history, radiation state or implement a surrogate.
