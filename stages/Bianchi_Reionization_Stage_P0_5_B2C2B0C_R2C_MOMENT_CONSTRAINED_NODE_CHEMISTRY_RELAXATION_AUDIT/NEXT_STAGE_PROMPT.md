# Authorized next stage prompt

@Web+Wolfram execute
`P0.5-B2C2B0C-R2C-R1-RATE-DERIVED-POSITIVE-MULTIRATE-RELAXATION-CONE-LOCK`.

Keep the R2A global/macro endpoint moments, the R2B fixed node support and
endpoint distributions, the R2C initialization projection, all exact photon
and nuclei ledgers, and the R2C fail-closed certificates canonical.

This is a model-adequacy preflight, not a production history.

1. Create the durable R2C-R1 directory, input lock, stage state, receipts,
   manifest and SHA256SUMS before calculation.
2. Replace the single common tau only at the model level. Define positive,
   separately auditable relaxation-rate families for `M`, `I`, `U`, `C`, and
   `J_g`; do not fit an unconstrained rate independently to every node.
3. Derive admissible rate intervals from the inherited reduced-DAE secants and
   available photoionization/recombination/heating/cooling/cycling terms. Mark
   any rate not physically identifiable as an interval nuisance parameter,
   not a calibrated quantity.
4. Test a one-mode positive kernel first. Only if it fails, test a bounded
   two-mode completely monotone mixture. Lock all mode-count and rate bounds
   before examining feasibility results.
5. Require the analytic trajectory, not only endpoints, to stay inside
   `M>=0`, `0<=I<=M`, `U>=0`, `C>=0`, `J_g>=0`, macro mass/volume caps, and
   `sum_g J_ig<=C_i`. Check analytic extrema or a prelocked certified
   collocation rule.
6. Preserve macro/group/global endpoint moments exactly. Use constrained KL
   projection only as an algebraic cone operator with KKT certificates; do not
   clip, infer cloud mass from opacity, or transport moments between macros.
7. Store primal feasibility, dual/Farkas certificates, active sets,
   rate-identifiability intervals, kernel weights, KL/TV work, and exact
   photon/H/He ledgers for all three shape priors.
8. Prelock absolute and relative integration-error tolerances and extend the
   refinement audit to at least `dt/2`, `dt/4`, and `dt/8` for any candidate
   that passes the analytic cone gate.
9. Keep G2b/G3 effective-HI and primary HeII/G3 exact zero.
10. Do not start unresolved subtraction, front/Q_M, source/fesc, primordial
    recombination adapter/surrogate, CAMB transfer, or Bianchi feedback.

Only an all-lane positive-kernel cone pass may authorize a subsequent
`R2C-R2 RATE-DERIVED NODE-CHEMISTRY HISTORY`. Otherwise preserve the no-go and
return a dual certificate identifying the missing physical degree of freedom.
