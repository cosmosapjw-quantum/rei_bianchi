# Next exact stage

Execute:

```text
P0.5-B2C2B0C-R2C-R1B-R2B-R2A-R2-R1A-R1-R1-R1-R1-
EVALUATION-SITE-SPARSE-GENERATOR-VALIDATED-MPRK22-SDIRK2-
DISCRETE-MAP-ENCLOSURE-LOCK
```

1. Keep the 26-event registry, MPRK22(1), Alexander SDIRK2, exact He II
   Ly-alpha heating, unresolved OTS ledger, canonical forcing, owner law, all
   prior endpoints, and the present local/global sparse contracts fixed.
2. Give every node independent `(v,f,vf)` generator blocks at the four locked
   source evaluation sites: `population_t0`, `population_t1_predictor`,
   `thermal_tgamma`, and `thermal_t1_final`.
3. Do not assume temporal smoothness or identify controls across sites unless a
   separately sourced branch-function regularity axiom is locked.
4. Differentiate the actual positive MPRK22 linear solves and safeguarded
   SDIRK2 thermal root, including state-dependent owner normalization. Retain
   local block support and the named low-rank global reductions.
5. Compute an outward interval/ellipsoidal remainder for all neglected
   cross-site and state-feedback terms. Rust may accelerate contractions only
   after Python containment and ULP parity pass.
6. Localize Hummer-Seaton knot crossings and restart the fixed-topology map.
7. The validated map must include: point-degenerate parity, 24 inherited static
   trajectories, the three-lane stagewise schedule witness, coherent-grid and
   node-local adversarial samples.
8. Close H/He nuclei, photon, resolved heat, unresolved OTS, escaped energy, and
   total-energy ledgers as set inclusions.
9. Authorize the uncertainty-qualified first canonical interval only if all
   three lanes are certified and every public width is below `2e-3`.
10. If evaluation-site remainder growth is uncontrolled, fail closed and open a
    source-regularity/calibration stage. Never impose global or temporal
    coherence post hoc.
