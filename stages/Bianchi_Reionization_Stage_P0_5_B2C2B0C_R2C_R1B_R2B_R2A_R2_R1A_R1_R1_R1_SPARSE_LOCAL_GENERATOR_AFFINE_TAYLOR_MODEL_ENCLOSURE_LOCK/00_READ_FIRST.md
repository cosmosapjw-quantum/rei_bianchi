# Read first

This stage closes the **instantaneous sparse source representation** and the
**low-rank owner-normalization coupling**, and validates an optional Rust
outward-bounds kernel against the Python oracle. It does **not** certify the
full MPRK22/SDIRK2 discrete map for the source-safe differential inclusion.

The decisive falsification is an admissible upper-to-lower branch schedule at
an explicitly localized half-step. It passes all thermochemistry and ledger
gates but its accepted endpoint lies outside the hull of all four static branch
corners in `x_HeIII`. A single fixed `(theta_v_i,theta_f_i)` pair per node and
substep is therefore incomplete without a validated remainder.

Read `RESULTS_AND_VERDICT.md`, `FORMALISM.md`, `PHYS_MATH_AUDIT.md`,
`PHYS_MATH_CODE_AUDIT.md`, and `NEXT_STAGE_PROMPT.md` before continuing.
