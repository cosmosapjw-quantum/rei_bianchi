# LCV-ODE N successor: bounded shadow guardrails

This additive stage is an external-audit candidate, not an active ODE stage.
It leaves every predecessor and historical artifact byte-identical and is not
imported by the adaptive-history driver.

Implemented scope is deliberately narrow:

- exact finite-rational algebra and outward binary64 export;
- exact point-linear-system certificates with an independent replay oracle;
- an explicit per-hydrogen opacity convention, one helium-abundance conversion,
  and direct nonnegative absorber shares;
- typed admission and a terminal-absorbing controller state machine;
- an allowlisted, bounded audit-run recorder.

Unsupported transcendental functions, genuinely interval-valued linear
systems, unresolved vacuum policy, missing solver evidence, and illegal state
transitions fail closed. See `SCIENTIFIC_CONTRACT.md` and
`FINAL_ODE_INTEGRATION_AUDIT.md` for the exact claim ceiling.

