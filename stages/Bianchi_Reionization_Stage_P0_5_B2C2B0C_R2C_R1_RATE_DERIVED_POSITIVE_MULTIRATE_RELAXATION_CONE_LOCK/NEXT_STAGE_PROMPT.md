# Authorized next stage prompt

@Web+Wolfram execute
`P0.5-B2C2B0C-R2C-R1A-NODE-LOCAL-PHYSICS-DERIVED-RATE-FIELD-CONE-PREFLIGHT`.

Keep R2A global/macro moments, R2B fixed node support/endpoints, the R2C
initial-current projection, exact photon/H/He ledgers, the R2C fail-closed
history audit, and every R2C-R1 rate lock/Farkas/KKT/trajectory certificate
canonical.

R2C-R1 rejected only the macro-shared common-equilibrium rate family. Test the
least-complex missing freedom before introducing a coupled generator:
deterministic node-local physical rate fields. This is still a model-adequacy
preflight, not production chemistry.

1. Create the new durable R2C-R1A directory, input lock, stage state, receipts,
   manifest, and `SHA256SUMS` before calculation.
2. Do not widen any R2C-R1 rate bound from the post-result dual diagnostic.
   Preserve `dual_single_bound_extension_diagnostic.csv` as non-authorizing.
3. Derive node-local positive rate evidence at both inherited endpoints from
   explicit local fields, with units retained until the final `Myr^-1`
   conversion:
   - `M`: local gross diffuse/sink transfer turnover;
   - `I`: local photoionization, recombination, collisional ionization, and
     transferred ionized-H activity;
   - `U`: local photoheating, cooling, expansion, and transferred thermal
     energy;
   - `J_g`: `c(1+z) kappa_{ig}/Mpc`, endpoint secants, and every explicitly
     inherited group-boundary/source term;
   - `C`: only an independently derived local cycling/recombination law. If no
     such law exists, mark it `UNIDENTIFIABLE_REQUIRED_RATE`; do not invent a
     nuisance value.
4. Node dependence must be deterministic from physical inputs. Do not optimize
   one unconstrained rate per node. If uncertainty scaling is necessary, use a
   small predeclared macro-shared hyperparameter vector and lock its bounds
   before any cone result is inspected.
5. Audit support tails without dropping tiny-weight nodes. Record finite,
   zero-support, floor-sensitive, and unidentifiable rows separately. Require
   dimensional consistency, nonnegative rates, and exact support-key joins.
6. Before feasibility, write and commit a node-local rate-field lock and prove
   that its weighted macro reductions reproduce the inherited macro process
   evidence within a prelocked tolerance. Any discrepancy is fail-closed.
7. Then test one-mode endpoint/equilibrium and analytic cone feasibility using
   the fixed local field or its macro-shared hyperparameters. Do not add a
   second mode in this stage; R2C-R1 already established that mode count is not
   the first missing freedom.
8. Preserve exact macro/group/global endpoint moments, current-Gamma, H/He
   nuclei identities, macro mass/volume caps, and
   `J_G1+J_G2a<=C`. No clipping, cloud-mass inversion, dynamic KL repair, or
   inter-macro moment transport.
9. Emit self-contained primal/KKT or Farkas certificates. Wolfram must check
   local-rate formulas, dimensions, endpoint identities, and structural zeros;
   if unavailable, retain an executable `.wl` script and an independent
   high-precision fallback.
10. Do not start production node chemistry, unresolved subtraction, front/Q_M,
    source/fesc calibration, primordial recombination adapter/surrogate, CAMB
    transfer, or Bianchi feedback.

Decision rule:

- If all three shape lanes pass with deterministic node-local physics-derived
  rates and no free node fitting, authorize an R2C-R1B finite-history audit.
- If the local field is identifiable but the coupled current/capacity or mass
  cone remains infeasible, authorize instead
  `R2C-R1B-PHYSICS-DERIVED-COUPLED-POSITIVE-GENERATOR-CONE-LOCK`, using
  `q_gamma=(C-J_G1-J_G2a,J_G1,J_G2a)` and
  `q_M=(M,M_cap-M)` with a prelocked Metzler topology and physically derived
  off-diagonal rates.
- If required local rates are unidentifiable, fail closed and return the
  missing source/operator terms; do not promote a fitted surrogate.
