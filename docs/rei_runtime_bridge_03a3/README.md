# REI-RUNTIME-BRIDGE-03A3 — Authority-Binding GREEN Closeout

## Exact tested payload

```text
Draft PR #47
commit c88463e63190eed25a1d6eeb03d0868d0013ed40
tree   1db10cccddfe47cff7cc202f7ccf3305e21ae05a
```

The exact payload passed the dedicated authority-binding workflow, the inherited import-firewall workflow, the former RED contract, the independent closed-package verifier, and the repository verifier.

## Earned source claim

```text
PASS_AUTHORITY_BINDING_SOURCE
```

The source now fixes the GitHub authority, binds the executing package to the exact verified checkout, binds preflight observations and paths, requires a server-protection receipt, re-attests the complete successor toolchain immediately before reservation, and retains the post-lease worker boundary.

## Strictly withheld

```text
ATTEMPT_REF_SERVER_PROTECTION   NOT_YET_ATTESTED
TARGET_HOST_STATIC_PREFLIGHT    NOT_RUN
GLOBAL ATTEMPT REF              ABSENT
LOCAL LEASE                     NOT_CREATED
NATIVE RUNTIME                  NOT_RUN
FIRST CANONICAL INTERVAL        NO_PASS
PROVIDER EXPORT                 NOT_AUTHORIZED
SCIENTIFIC PASS                 NOT_CLAIMED
```

## Dependency boundary

BASS PR #116 provides a source-level formula-consumer role graph but explicitly has no local runtime pass, consumer parity, generic REI Bianchi direction-flow implementation, J/G closure, screen transport, provider, or science promotion. REC PR #55 provides the trusted RF-00 payload-gate handoff but has not admitted REC source integration, grid/PSTF numerical parity, a physical face, or a provider.

## Next node

```text
REI-RUNTIME-BRIDGE-03A3-R_ATTEMPT_REF_SERVER_PROTECTION
```

That node must establish and read back update, deletion, and non-fast-forward protection for `refs/heads/attempt-ledger/**` and emit the exact protection receipt expected by this source. It must not create the attempt ref or run the native worker.
