# rei_bianchi first canonical interval bootstrap

This package uses the reviewed adaptive-history scaffold already present on the
2026-08-23 audit branch. The historical corrected-ODE candidate remains
TEST_ONLY/HOLD and is not activated.

The exact rec main is now monitored. The first interval consumes no rec
numerical history and may proceed. A future primordial recombination splice is
hard-blocked until rec earns its provider export and the adapter review passes.

Exact next action: `REI-BOOTSTRAP-00`, then one endpoint reproduction and the
full first interval in the same local run if the endpoint gate passes.
