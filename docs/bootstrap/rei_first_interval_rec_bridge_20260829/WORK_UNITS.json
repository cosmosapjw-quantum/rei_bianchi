{
  "schema": "rei-bianchi-first-interval-work-units/v1",
  "exact_next_action": "REI-BOOT-00",
  "work_units": [
    {
      "id": "REI-BOOT-00",
      "kind": "CURRENT_MAIN_AND_AUDIT_RECONCILIATION",
      "actions": [
        "create an isolated worktree from the package branch's exact current-main base",
        "run current repository bootstrap, stage verifier and current-stage tests",
        "read the audit branch and corrected ODE candidate as evidence only",
        "freeze the existing rec lock without updating it"
      ],
      "terminal": ["READY_REI_FIRST_INTERVAL", "BLOCKED_SOURCE_OR_AUDIT_IDENTITY"]
    },
    {
      "id": "REI-AUDIT-RED-01",
      "kind": "PORT_ONLY_REPRODUCED_ODE_FINDINGS",
      "actions": [
        "extract each still-applicable audit finding",
        "write a current-main RED that reaches the numerical path",
        "reject findings that do not reproduce",
        "do not count import, packaging or stale-hash failures as numerical evidence"
      ],
      "terminal": ["RED_RETAINED", "NO_APPLICABLE_FINDING", "BLOCKED_AUDIT_AMBIGUITY"]
    },
    {
      "id": "REI-HISTORY-02",
      "kind": "FIRST_CANONICAL_INTERVAL_ADAPTIVE_HISTORY",
      "actions": [
        "start from partition 2048",
        "compute one full and two half images for every attempt",
        "accept only when implicit, positivity, width, structural-ledger and local-error gates pass",
        "bisect only failed attempts to the locked minimum",
        "localize every Hummer-Seaton/table knot before commit",
        "run all three lanes without favorable selection",
        "track generator rank, owner low-rank modes, remainder growth, event distance and transactions",
        "close the seven ledgers over the complete interval"
      ],
      "terminal": ["PASS_REI_FIRST_CANONICAL_INTERVAL", "BLOCKED_INTERVAL_GATE"]
    },
    {
      "id": "REI-REC-LOCK-03",
      "kind": "READ_ONLY_DEPENDENCY_GATE",
      "actions": [
        "keep the existing rec lock byte-identical",
        "if no accepted rec interface receipt exists, record LOCK_HELD_PENDING_REC",
        "do not block the rec-independent first-interval result",
        "defer adapter and lock update to a separate work unit"
      ],
      "terminal": ["LOCK_HELD_PENDING_REC", "READY_FOR_REC_ADAPTER_REVIEW"]
    },
    {
      "id": "REI-DELIVER-04",
      "kind": "BOUNDED_REVIEW_AND_DRAFT_PR",
      "actions": [
        "one fresh review",
        "at most one reproduced P0/P1 repair",
        "ordinary push, one draft PR and exact remote readback"
      ],
      "terminal": ["DELIVERED_DRAFT", "BLOCKED_REMOTE"]
    }
  ],
  "edges": [
    ["REI-BOOT-00", "REI-AUDIT-RED-01"],
    ["REI-AUDIT-RED-01", "REI-HISTORY-02"],
    ["REI-HISTORY-02", "REI-REC-LOCK-03"],
    ["REI-REC-LOCK-03", "REI-DELIVER-04"]
  ],
  "forbidden_global": [
    "promotion or wholesale merge of the STOP_INVALID candidate",
    "silent rec lock update or surrogate recombination input",
    "clipping, table extrapolation or favorable lane selection",
    "dense full Jacobian",
    "recombination splice, CAMB transfer or Bianchi sweep",
    "timing, GPU, Wolfram, unrelated full-suite reassurance",
    "merge or ready transition"
  ]
}
