# rei_bianchi first canonical interval bootstrap with rec dependency firewall

This package turns the ODE four-loop audit into current-main regression work without promoting the historical corrected candidate. It preserves `rec_bianchi` as an explicit scientific dependency.

## Exact audit evidence

```text
audit branch: audit/ode-four-loop-external-20260823
audit commit: ace7d91af35bfefcc3a9bd7e83076aa8f8bf557e
audit tree:   c8167922076f52628b1f7243c9ebd8b40ebe7508
candidate verdict: STOP_INVALID / HOLD
```

The implementation base is the parent of this package stack, created from live `main`. The audit branch and corrected ODE candidate are read-only evidence; they are not a wholesale merge source.

## Executable next science stage

The current durable result is one accepted four-site FLRW thermochemistry microstep at partition 2048. The next rec-independent result is the complete first canonical interval with adaptive full-versus-two-half error control, all three lanes, table-event restart transactions and all seven ledgers.

## rec_bianchi dependency

The existing rec lock remains byte-identical. The first canonical FLRW interval may proceed, but recombination splice, hydrogen-frame adapter, CAMB transfer and Bianchi-family work remain blocked until `rec_bianchi` emits `PASS_REC_SPLIT_DOMAIN_REPLACEMENT_AND_INTERFACE_V1` and this repo performs a separate adapter/input-lock review.

Read `PACKAGE.json`, `AUDIT.md`, `REC_REI_INTERFACE_BRIDGE.json`, `WORK_UNITS.json`, and `CODEX_HANDOFF.md`.