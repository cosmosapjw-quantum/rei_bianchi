# Codex handoff — rei_bianchi first canonical interval

```text
repository: cosmosapjw-quantum/rei_bianchi
source branch: audit/ode-four-loop-external-20260823
source HEAD: ace7d91af35bfefcc3a9bd7e83076aa8f8bf557e
source tree: c8167922076f52628b1f7243c9ebd8b40ebe7508
intended implementation branch:
agent/implementation/rei-first-canonical-interval-20260829-r1
exact next action:
REI-AUDIT-COMPAT-00 then REI-INTERVAL-02 in the same run
```

Use a new isolated worktree. Preserve every current checkout and artifact.
Never clean, reset, stash, amend, rebase, squash or force-push.

## Intake

Run the repository verifier and current-stage validators from the exact source.
Read:

1. `handoff/CURRENT_HANDOFF_PROMPT.md`
2. `PROJECT_STATE.json`
3. `docs/science/current_00_READ_FIRST.md`
4. `docs/provenance/DURABLE_STAGE_LEDGER.csv`
5. `external/REC_BIANCHI_MONITORING_POLICY.md`
6. this package

## Audit compatibility — bounded

Do not copy the 8/23 additive ODE candidate into the active route. Reproduce
its review findings against active first-interval dependencies. Fix only an
actually reproduced active defect. The compatibility pass plus at most one
repair is the complete audit scope. If it closes, immediately continue to the
interval in this same run.

Update the rec monitoring lock to exact rec main `5a09f3797210284f83a1a1adb0e0092d1ac48475`, audit checkpoint `4cd2c7bff00ca91c57997d7e6e1ff4c67f7fccd3`, and bootstrap package `47e19df30a5e71e536d3d5167ffa3b78638a59c5` / tree `e7400fda8511e582f5bc4944b96183977412f4f3` (PR #31). This is metadata/checkpoint consumption only.
Do not import rates, populations, histories, source-bound verifiers or a
surrogate.

## First canonical interval

Start from the certified partition-2048 microstep. For every attempted step and
all three lanes compute one full and two half images. Apply the existing
implicit, positivity, width, structural-ledger and local-error gates. Bisect
only the failed attempted step to the already locked minimum; do not invent a
new minimum.

Localize each Hummer–Seaton knot before commit. Failed attempts and event
crossings must preserve parent state/ledger bytes transactionally. Track rank,
owner modes, remainder growth, event distance and accepted/rejected attempts.
Close all seven ledgers over the complete interval.

Use an independent replay/state-observable oracle. Do not use a self-reported
residual as its own reference and do not compare secondary error estimates with
an unjustified error-of-error threshold.

Run one PHYS-MATH and one PHYS-MATH-CODE review, repair at most one reproduced
P0/P1 defect, ordinary-push one draft PR, exact remote readback, and stop.

Terminal ceiling:

```text
PASS_REI_FIRST_CANONICAL_INTERVAL_ADAPTIVE_HISTORY_FLRW_THERMOCHEMISTRY
```

This does not authorize rec splice, production chemistry, CAMB, Bianchi
feedback or a family sweep.
