# Codex handoff — rei_bianchi first canonical interval R2

```text
repository: cosmosapjw-quantum/rei_bianchi
source branch: audit/ode-four-loop-external-20260823
source HEAD: ace7d91af35bfefcc3a9bd7e83076aa8f8bf557e
source tree: c8167922076f52628b1f7243c9ebd8b40ebe7508
implementation branch: agent/implementation/rei-first-canonical-interval-20260829-r1
exact next action: REI-AUDIT-COMPAT-00 then REI-INTERVAL-02 in the same run
```

Use a new isolated worktree. Preserve every checkout, untracked file and durable artifact. Never clean, reset, stash, amend, rebase, squash or force-push.

## Exact rec checkpoint — metadata only

```text
repository: cosmosapjw-quantum/rec_bianchi
package branch: agent/plans/rec-split-domain-bootstrap-20260829-r1
immutable package commit: 47e19df30a5e71e536d3d5167ffa3b78638a59c5
immutable package tree: e7400fda8511e582f5bc4944b96183977412f4f3
PR: #31 OPEN / DRAFT / UNMERGED
```

Verify the Git objects independently. Consume checkpoint metadata only. Do not import rec source-bound validators, rates, populations, histories or a recombination surrogate. This first interval may run in parallel with rec split-domain implementation; a primordial-to-CMB splice may not.

## Intake

Read, in order:

1. `handoff/CURRENT_HANDOFF_PROMPT.md`
2. `PROJECT_STATE.json`
3. `docs/science/current_00_READ_FIRST.md`
4. `docs/provenance/DURABLE_STAGE_LEDGER.csv`
5. `external/REC_BIANCHI_MONITORING_POLICY.md`
6. `PACKAGE_R2.json`
7. `ODE_AUDIT_COMPATIBILITY_R2.json`
8. `WORK_UNITS_R2.json`

Run the repository verifier and only current-stage validators. Do not rerun historical campaigns for reassurance.

## One-pass active ODE compatibility

Do not activate or copy the 8/23 additive ODE candidate wholesale. Reproduce its review findings against the code paths actually invoked by the first-interval runner. Fix only a defect that reaches a targeted assertion. Import, collection or missing-file failure is not scientific detection evidence.

The compatibility pass and at most one repair are the whole audit scope. If it closes, continue immediately to the interval in the same run.

## Complete first canonical interval

Start from the certified partition-2048 four-site microstep. For every attempted step and all three lanes compute one full image and two half images. Apply the existing implicit, positivity, width, structural-ledger and local-error gates. Bisect only the failed attempted step to the already locked minimum; do not invent a new minimum or widen a gate after seeing data.

Localize every Hummer-Seaton table knot before commit. A rejected attempt or event crossing must leave parent state and ledger bytes transactionally unchanged. Track sparse-generator rank, named owner modes, remainder growth, event distance and accepted/rejected transactions. Close all seven ledgers over the full interval.

Use an independent replay/state-observable oracle. Do not use a production-reported residual as its own reference and do not compare secondary error estimators with an unjustified error-of-error threshold. Judge numerical output by state/observable agreement, conservation, positivity, event/restart invariants and convergence.

Run one PHYS-MATH and one PHYS-MATH-CODE review, repair at most one reproduced P0/P1 defect, ordinary-push one draft PR, read back exact head/tree/paths/evidence and stop.

Terminal ceiling:

```text
PASS_REI_FIRST_CANONICAL_INTERVAL_ADAPTIVE_HISTORY_FLRW_THERMOCHEMISTRY
```

This does not authorize rec splice, production node chemistry, CAMB transfer, Bianchi feedback, family sweep, timing/GPU/Wolfram, merge or ready transition.
