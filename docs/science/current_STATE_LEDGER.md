# State ledger

Attempts 1--9 are preserved under `state/`. Attempt 10 exceeded the combined all-lane runtime after the primary coarse history had completed. The isolated diagnostics then established the hard physical failure without requiring the remaining sensitivity lanes.

Authoritative scientific outputs are the failure verdict, gate summary, first-interval refinement table, coarse diagnostic history/ledger, geometry call audit, symbolic divergence result, and next-stage prompt. No node history is accepted as production.
