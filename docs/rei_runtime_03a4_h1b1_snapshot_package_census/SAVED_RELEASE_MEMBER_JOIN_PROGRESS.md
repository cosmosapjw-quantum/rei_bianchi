# Saved Release/member join

Task layers: implement, validate, review, document. LOCAL_CODEX is the sole writer.

Parent and sibling commit/tree identities and exact reference blobs were verified. The supplied byte-intake results are reused; no old successful suite was replayed for intake. The original checkout remains unchanged.

Prepared five integrated public-consumer methods and the exact sibling seven compatibility methods. Synthetic fixture clock and diagnostic stderr changes are separate from production compatibility; all original 18 assertion methods are AST-identical. The discriminator will use one saved, public, signed synthetic fixture before and after the donor amendment. J01 alone is the intended RED selection; the other four methods are unselected, not skipped or counted as failed feature tests.

Only Release SHA256 row grammar gains literal @; package path, signature, signer, hash/size and production date policy remain unchanged. The #70 member consumer and its 15-test source stay byte-identical.

Next executable action: generate the public fixture once, run J01 against the historical donor, then apply the exact amended donor and run frozen child regression.

## Actual RED and exact amendment

RED commit `d804be14eb5ba194fc9ac8c0bc2b32cdd14a315f`, tree `0bba8fe9991e662536f62637f9567b0125971c74`: J01 made one actual donor call through the public consumer and failed with `MISSING_RELEASE_MEMBER_JOIN:UNSAFE_ARCHIVE_PATH`. One assertion failure, zero errors/skips, four explicitly unselected join methods. Policy class identity was verified; this is not an import or fixture failure.

The saved public fixture manifest SHA-256 is `e3e3ff41f9dfeef7095123ef110ef3e0540420dde0ce198b6df4fc4cc3851045`. Its signed bytes will be reused unchanged for GREEN. Applied only the exact sibling donor `dfed33dd8428cd3293a8292088c21ba23f6b2f43`; package-path/signature/hash/date code is unchanged. Next: frozen join, compatibility, member, donor and repository checks against this child.
