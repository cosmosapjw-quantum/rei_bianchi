# REI thread-state consolidation — 2026-09-05

## Purpose

This append-only package preserves the `rei_bianchi`-specific research, mathematics, runtime-governance, host-epoch, verification, ownership, and DAG results developed in the current project thread.

It is intentionally stacked on the latest directly verified REI mathematics head and does **not** merge the divergent runtime branch. Runtime results are included by exact pull-request, commit, tree, workflow, receipt, and live-server pins.

## Exact publication base

```text
repository  cosmosapjw-quantum/rei_bianchi
base PR     #62
base branch research/rei-math-m1-spatial-curvature-green-20260904-r1
base commit 01fd5ea775795d27758f354971ca478f90701295
base tree   d802eed60d98e5f2c32189ca0d358cb4f084df09
```

Divergent runtime authority is pinned rather than merged:

```text
runtime PR     #59
runtime branch audit/rei-runtime-03a4-host-epoch-lineage-closure-red-20260904-r1
runtime commit 00d17c932eb41dbae6467e1e2fdf46818799d6db
runtime tree   4752300f2715fba6368811204d159a5d4c2f6465
```

## Source-of-truth precedence

1. Fresh direct GitHub object, workflow, ruleset, and ref readback.
2. Exact repository artifacts and exact pull-request bodies bound to Git objects.
3. Operator-produced durable receipts and transcripts, with their evidence class stated explicitly.
4. This thread synthesis.
5. SciSpace literature, which has methodology-only authority.

A summary, filename, branch name, or successful parse is not byte identity. A local or operator-reported pass is not relabelled as an exact-head GitHub execution pass.

## Consolidated state

### Formula lane

The generic homogeneous spatial Ricci tensor, Ricci scalar, and homogeneous STF divergence are exact-head symbolic GREEN at PR #62. The class-B mixed `epsilon*n*a` sign is independently mutation-sensitive. The four-dimensional Einstein momentum/Codazzi sign, Hamiltonian/momentum constraint bridge, constraint propagation, and background numerical evolution remain separate unresolved nodes.

### Runtime lane

The undeclared `ntpath` import-root defect is source-closed. Production import is behind both global and local leases; authority and canonical runtime paths are source-bound. Repository ruleset `22240889` is active for `refs/heads/attempt-ledger/**`, and the fixed final-attempt ref is absent on fresh readback. H1A Docker isolation evidence is durably recorded, but the historical host epoch has not been reconstructed. No successor Section-0, static target-host preflight, global lease, persistent local lease, native dispatch, first interval, or provider admission has occurred.

### Ownership

REI owns late reionization, opacity, thermochemistry, OTS event logic, first-canonical-interval evidence, and provider values. BASS owns shared Bianchi geometry/background, global matter-frame tilt, photon transport, and representation certificates. REC owns primordial recombination source microphysics. HTT owns local-observer sky pullback and processed observational response. REI formula work that recomputes shared geometry is an independent oracle with `authority_effect=NONE` over BASS ownership.

## Package map

- `CURRENT_STATE.json` — machine-readable canonical state.
- `PROGRESS.md` — before/after durable checkpoint.
- `THREAD_SCOPE_AND_PROVENANCE.md` — inclusion, exclusion, and evidence classes.
- `MATHEMATICS_AND_PHYSICS.md` — formula results, conventions, limits, and open mathematical gates.
- `RUNTIME_GOVERNANCE_AND_HOST_EPOCH.md` — runtime, ruleset, compiler provenance, attempt budget, and next host-epoch node.
- `DEPENDENCY_AND_OWNERSHIP.md` — BASS/REC/REI/HTT interfaces and firewalls.
- `DAG.md` — synchronized formula and runtime DAG.
- `CLAIM_LEDGER.csv` — atomic status ledger.
- `EVIDENCE_INDEX.json` — exact GitHub, operator, Atlassian, and literature locators.
- `OPERATOR_EVIDENCE_EXCERPTS.md` — normalized load-bearing transcript facts.
- `SCISPACE_WOLFRAM_METHOD_LOCK.md` — external-method evidence and current Wolfram no-result boundary.
- `PHYS_MATH_AUDIT.md` and `PHYS_MATH_CODE_AUDIT.md` — independent audit ledgers.
- `AUDIT_COMPILED_WORK_UNIT.json` — executable admission contract.
- `CODEX_HANDOFF_PROMPT.md` — next bounded local task.
- `validate_package.py` and `SOURCE_INDEX.json` — closed Git-blob package verification without a self-hash cycle.

## Strict claim ceiling

```text
M1 spatial-curvature formula             PASS
M1 class-B sign mutation                 PASS
spacetime Gauss-Codazzi sign             DEFERRED
constraint propagation                   NOT_RUN
historical host epoch                    NOT_RECONSTRUCTED
successor Section-0                      NOT_RUN
target-host static preflight             NOT_RUN
global attempt ref                       ABSENT
persistent local lease                   NOT_CREATED
remaining native attempts                1
native runtime                           NOT_RUN
first canonical interval                 NO_PASS_FIRST_CANONICAL_INTERVAL
provider export                          NOT_AUTHORIZED
scientific pass                          NOT_CLAIMED
merge / ready transition                 NOT_AUTHORIZED
```

This package documents and verifies state. It changes no REI physical equation, thermochemistry value, production bridge, Rust numerical source, runtime lock, provider output, or attempt state.
