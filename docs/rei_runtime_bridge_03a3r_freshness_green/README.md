# REI-RUNTIME-BRIDGE-03A3-R1 — Fresh protection and live revalidation GREEN candidate

## Exact ancestry

```text
PR #47 authority closeout
b7d591da8554bb17cf395d5388a5f0649ebf754f

PR #48 expected RED
b8cd8b828c2517c537860f856792cae53f72b967
```

## Intended bounded result

The successor package rejects stale, expired, unbounded, or materially
future-dated source protection receipts.  After the complete thirteen-field
successor-toolchain re-attestation, it performs fixed-authority GET-only reads
of the prospective branch rules, contributing active ruleset details, and the
exact global-ref absence.  The live and source protection hashes are bound into
the global receipt and revalidated by the post-lease worker.

## Strict boundary

This source candidate does not create a GitHub ruleset or global attempt ref.
It creates no local lease, dispatch intent, native result, first interval,
provider output, or scientific claim.  The repository ruleset remains a
separate administrative prerequisite after exact-head GREEN.
