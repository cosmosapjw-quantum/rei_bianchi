# Codex: rei_bianchi source-bound paired-map continuation

## Start from the actual published objects

Use the exact payload and terminal receipt commits supplied in the external
entry prompt. Materialize the direct files and run `verify_payload.py` once
before mutation. A moving branch tip alone is not a scientific identity failure.
The immutable source is PR14 at
053b97c56e089e28a83f37d79a4128ed3cdae9f4 / 46a96c789a691d671644685893a552cd9486788d.
Its STOP_INVALID verdict is preserved. The original helper is at
82c67218248cb896019b2bffc590da1260a214fc / dc801c78f01be32f6e6d74cc2d3f2abcfe2279d2.

Create a new isolated implementation worktree from the terminal delivery
commit. Preserve all existing worktrees, untracked prototypes and rejected-run
bytes. Never reset, clean, stash, amend, rebase, force-push, merge or mark ready.
Do not run an old publisher or create a duplicate package PR.

Read CONTRACT.json and the PR14 reviews. The newer PR14 STOP_INVALID result
takes precedence over the old one-microstep success in the source handoff.
Preserve the blocked-run archive with SHA-256
`a861278201313c55e08ba6323b5c1d2ad97bf5765f429807b4eba0a1c2465d0b`.
If its /tmp copy is gone, find its unique basename
`first_interval_r2_blocked_minimum_step.tar.gz` in the exact PR14 Git tree and
materialize those immutable bytes. Do not rerun R2 merely to recover artifacts.

## REI-LOCAL-01_SOURCE_BOUND_PAIRED_MAP_ADAPTER

Only paired_budget.py is delivered as numerical code. It subtracts DECLARED
shared affine coefficients and ADDS independent remainder radii. It neither
constructs a map nor proves source correlations, implicit roots, or uniform
nonlinear enclosures. Its strict comparison flag is conditional, never the
production acceptance flag. Earlier undelivered shared_discrete_map.py code or
unverified historical pass counts are not authority.

Implement the actual MPRK22/SDIRK2 source-bound paired adapter in a new
`src/rei_bianchi/correlated_map_adapter.py` and directly needed local modules,
tests and a new R3 stage. Required import/registration additions for this path
are in scope. Keep old source equations, rates, tables and evidence untouched.
Full and first-half share the parent realization; second-half inherits all
first-half dependency and residual information. Distinct physical source sites
remain independent unless source authority establishes their relation.

Bind populations/log_T, event fluxes, denominators, predictor/corrector and
the WHOLE thermal residual. Temperature-dependent photoheating/context belongs
in its total derivative or a validated outer self-inclusion proof. Midpoint
Jacobians and a point solver proposal are not uniform certificates.
Treat H-F before projection to intervals, including a justified joint nonlinear
remainder. No cancellation may be invented from equal bounds or from unnamed
remainders. A direct difference-remainder path must be explicit and independently
proved; the delivered helper always adds its two remainder radii.

Use a small independently solved nonlinear fixture and real numerical RED
first; then bind to all actual source nodes. Sparse/local H/He blocks may be
reused, but a generic VigilODE port or new router is NOT a predecessor.

## REI-LOCAL-02_ALL_NODE_THREE_LANE_UNIFORM_PILOT

Run the prospective [160,161] pilot on all 46080 nodes and all three lanes.
Retain strict local <2e-4, strict public width <2e-3, and the one-tick minimum
using the source binary64 constants. No width subtraction, midpoint-only
estimator, post-hoc uncertainty narrowing, clipping or Richardson-factor change.
A saved parent-box lift is a conservative superset, not recovered covariance.
Save both endpoints, shared dependency, remainder/implicit certificates, old
Cartesian bound and all acceptance/rejection diagnostics. Resource exhaustion
is UNRESOLVED, not a proved inaccurate map. A sound bound can still fail.

## REI-LOCAL-03_ORIGINAL_START_R3_INTERVAL_ONLY_AFTER_PILOT_PASS

After source-map proof and pilot success, execute changed R3 dependencies from
the ORIGINAL initial state. Do not append to R2 or relabel its accepted rows.
Read original total_ticks/base_segment/max_depth from source, not guessed
values. Preserve table-knot localization, atomic all-lane acceptance, rollback
and restart with the complete dependency state. Verify a split-run restart.
Seven whole-history ledgers need simultaneous/integrated closure; marginal
residual intervals merely containing zero are not sufficient.

## Closeout and boundaries

Reuse valid unchanged evidence. Run only directly affected tests and changed
physics, then one PHYS-MATH and one PHYS-MATH-CODE review, at most one reproduced
P0/P1 repair and differential retest. No new audit loop for reassurance.
The old P2 descendant-timeout flake is disclosed and outside this numerical task.
The two stale old manifest bindings stay PROVENANCE_REBIND_REQUIRED; use this
raw-byte direct-object intake rather than rewriting old expected hashes.
Keep external/rec_bianchi.lock.json unchanged. No rec or BASS mutation, numerical
rec import, surrogate, production chemistry, CAMB, Bianchi feedback/family sweep,
GPU/Wolfram/timing, merge, ready or performance promotion.

Ordinary push one stacked draft implementation PR against the delivery branch.
Read back actual head/tree/paths/checks. Current NO_PASS_FIRST_CANONICAL_INTERVAL
can be replaced only by the proved full-interval terminal
PASS_REI_FIRST_CANONICAL_INTERVAL_ADAPTIVE_HISTORY_FLRW_THERMOCHEMISTRY.
Otherwise keep the failed candidate and report its exact remaining blocker.
